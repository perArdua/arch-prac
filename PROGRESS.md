# 진행 로그 — 자율 진행 중 내린 결정

> 2026-09-03 10:05~ 자율 작업 구간. 사용자 부재 중 내린 판단을 근거와 함께 기록한다.
> **동의하지 않는 항목은 되돌리기 쉽도록 근거를 명시했다.**

---

## 일어나셨을 때 — 한눈에

**만들어진 것**
```
(한 장 요약본)       한 장 요약 (5분) ← 여기부터 · 이 저장소에는 올리지 않음
README.md           진입점, 읽는 순서
docs/ 00~16         리서치·문제정의·설계·실측·반론·설계요소점검 (17편)
experiments/        실험 31개 (13~19 중 6개는 Gemini 무료 티어)   🔄 (2026-09-11) 33개
docs/adr/           결정 기록 16편 + 근거 감사 색인
eval/               사실 대장 · 평가 문항 44개 · 합성 코퍼스 720턴 + 수기 세션 5개
experiments/        실험 31개 + 러너 + 결과 스냅샷 (25개는 키 없이 실행)   🔄 (2026-09-11) 33개 · 27개
prototype/          추천 구조 구현 — `demo/fact_demo/interpret_demo` + ⭐ `soak.py`
```

**핵심 결과 5가지**
0. 🆕 **대화를 직접 써보다 발견한 것 두 개**
   - **U10** — 유저가 틀리게 말했을 때 **동조**하는 실패. 제 실패 목록 9개가 전부 불만 게시물에서 왔는데,
     **유저는 맞장구를 불만으로 안 씁니다.** 조사 방법의 편향이 뭘 놓치게 했는지 찾은 사례입니다
   - **해석은 한 번에 안 생긴다** — S19에 *"세 번째다"*라고 써놓고 **근거가 없다는 걸 깨달아** S05·S13을 추가로 썼습니다.
     그러자 진행이 보였습니다: 1번째 말줄임표 → 2번째 말 삼킴 → 3번째 명명.
     → **L8에 최소 근거 수 정책 추가.** 1회 관찰로 해석을 만들면 성급한 일반화가 됩니다
0-b. 🆕 **구현해보니 설계 결함 세 개 더** ([docs/13](docs/13-prototype.md))
   - 어미 정규화 없이는 **정답이 탈락**. 문서엔 *"형태소 분석 필수"*라 써놓고 코드는 bigram으로 대충 했습니다
   - **"결정적 주입"을 "앞쪽 배치"와 혼동.** 조건부 존재 블록이 접두사를 깨뜨립니다 (57% → 100%)
   - **두 방어선의 공을 잘못 돌렸습니다.** 게이트가 막은 걸 하드 게이트 공으로 썼습니다
   → **provenance의 첫 실제 효용이 "제 주장의 오류를 잡은 것"**이었습니다
   - ⭐ **`superseded_by` 컬럼 하나로 되는 줄 알았습니다.** bi-temporal 무효화를 구현하려니
     술어마다 **카디널리티**(one/many)와 **가변성**(high/mid/low)을 선언해야 했고 규칙을 **세 번** 고쳤습니다.
     근본 원인: *"이직했어"*(세상이 변함)와 *"우리 오빠가"*(잘못 앎)가 **둘 다 "기존과 다른 값"**이라
     confidence로는 구별이 안 됩니다. → **`held`(보류)가 U10의 데이터 계층 방어선**입니다
   - ⭐ ***"3회 이상이면 해석 생성"*이라 정해놓고 무엇을 3회 셀지를 안 정했습니다.**
     자유 서술 + 어휘 유사도로는 **어떤 임계값도 작동하지 않습니다** — 회피와 칭찬거부가
     둘 다 *"화제를 돌렸다"*로 끝나서, 낮추면 뭉치고 높이면 같은 패턴끼리도 안 뭉칩니다.
     → **`observation` 층 추가.** (계기, 반응, 극성) 삼중항으로 저장하니 결정적으로 갈리고
     **반증 처리도 그제서야 가능**해졌습니다.
     **"LLM이 필요하다"는 알던 것이고 "LLM에게 무엇을 시킬지"가 새로 나왔습니다**
0-c. 🆕 **추출 프롬프트를 쓰다 나온 것** ([docs/14](docs/14-extraction-prompts.md))
   *"추출 품질이 시스템 상한을 결정한다"*고 반복해 써놓고 **프롬프트를 한 번도 안 썼습니다.**
   쓰려니 **한 번도 열거 안 한 어휘 세 개**를 정해야 했습니다 — 술어 25개(카디널리티·가변성 포함),
   계기/반응 21개, 민감 범주 목록. 전부 *"둔다"*고만 하고 안 만들었던 것들입니다.
   - 🔴 **`realm` 판정이 원리적으로 애매합니다.** *"나 회사에서 깨졌어"*가 real인지 fiction인지
     유저가 연기 중인지에 달려 있고, **이게 민감 범주 필터와 직결**됩니다.
     → **애매하면 `real`로 보고 필터를 겁니다.** 비대칭 손실에서는 안전한 쪽으로. 완화책이지 해결은 아닙니다
   - **importance 채점 루브릭을 처음 명시**했습니다 — *"3개월 뒤에도 알아야 하는가"*.
     Generative Agents는 1~10 척도만 주고 루브릭이 없습니다
0-d. 🆕 **읽기 경로 프롬프트를 쓰다 나온 것** ([docs/15](docs/15-read-path-prompt.md))
   [09](docs/09-what-memory-is-for.md)에서 *"M11 운용 층이 없다"*고 지적만 하고 설계 안 했던 자리입니다.
   - **기억을 넣으면 모델이 쓰라는 압박을 받습니다.** 검색 정밀도로는 못 막고 **프롬프트 층에서 막아야** 합니다.
     → `[알고 있는 것]`(전제) / `[꺼낼 만한 것]`(후보) / `[갚아야 할 것]`(의무)로 구획을 나누고
     **"안 써도 된다"를 명시**합니다
   - 🔴 **제 두 결론이 충돌했습니다.** *"안 꺼내면 신호 0"*(09) vs *"억지로 쓰지 마라"*(FP-5).
     → **수동/능동 경로를 물리적으로 분리**해서 해소. M11·M12를 나눈 게 옳았는데
     **그땐 개념적 구분이었고 프롬프트를 쓰니 이유가 분명해졌습니다**
0-e. ⭐⭐ **그리고 그 15번 문서를 실제로 구현했더니** ([소크 테스트](prototype/soak.py))
   **`[알고 있는 것]` 블록이 구현에 아예 없었습니다.** 설계는 했는데 `build_context`가 안 만들었고,
   `facts_at()`이 별도 데모에서만 불려서 **구멍이 안 보였습니다.** 10줄 연결로 **근거 도달 22% → 56%.**
   - 🔴 **그 수정이 하드 게이트를 우회했습니다** — 트랩(중요도 0.05)이 평범한 턴에 들어갑니다.
     유사 서비스 A의 *"빵 유언"*을 제 손으로 재현했습니다. 방어선을 **검색 경로에만** 세웠기 때문입니다
   - 🔴 **importance로는 못 거릅니다.** 술어에 **상시성(standing)**이 필요했습니다 —
     *"중요한가"*가 아니라 ***"속성인가 사건인가"***. 카디널리티·가변성에 이어 **세 번째로 구현이 강제한 속성**입니다
   - ⚠️ **그리고 검색 경로의 순증 기여가 0이었습니다.** 아키텍처 복잡도의 대부분이 거기 있는데.
     (문항 18개고 남은 실패가 다중세션·시간추론이라 *"검색 불필요"*라는 뜻은 아닙니다)

1. **코드 몇 줄로 입력 비용 63%↓** — 배치 순서 + 청크 축출. 아키텍처 안 바꿈
2. **실험 25개 중 대부분이 제 문서를 고쳤습니다** — 부주의, 테스트로만 발견 가능, 반증 2건
2-b. 🔴 **그런데 실험 1~10은 전부 부품을 격리해서 쟀습니다.**
   검색기만 재면 `recall@5 = 70%`인데 **파이프라인 전체는 22%**입니다.
   **부품 벤치마크는 시스템 성능의 상한이지 예측치가 아닙니다.** 이걸 마지막에야 쟀습니다
3. 🔴 **추천안이 현행과 recall 동률(96%)**입니다. 채택 이유는 precision(38% vs 22%) 하나뿐이고,
   **그게 값을 하려면 "precision 실패가 recall 실패보다 비싸다"가 참이어야 하는데 미검증**입니다

**봐주셨으면 하는 것**
- 한 장 요약본의 🟢/🟡/🔴 신뢰 수준 — **🟢은 지금 실행 가능, 🔴은 측정 후 결정**
- [docs/12 예상 반론](docs/12-objections.md) Q3 — 이 추천 전체의 하중을 지는 가정
- 제가 임의로 내린 결정 [D-A~D-E](#자율로-내린-결정) — 동의 안 되시면 되돌리기 쉽게 근거를 붙였습니다

**막힌 것:** 응답 품질·페르소나·L3 실패는 **API 키 없이 측정 불가**. 예상 $30~50.

---

## 완료

| 시각 | 산출물 |
|---|---|
| ~10:20 | [ADR-001~010](docs/adr/) — 결정 기록 10편 |
| ~10:35 | [10-final-recommendation.md](docs/10-final-recommendation.md) — 처음 질문(«처음부터 다시 설계한다면»)에 대한 답 |
| ~10:40 | [README.md](README.md) — 진입점, 읽는 순서, 한계 명시 |
| ~10:45 | 낡은 문서 4편(01·02·03·05)에 갱신 표시 — **덮어쓰지 않고 표시만** |
| ~10:55 | [eval/fact-ledger.yaml](eval/fact-ledger.yaml) — 사실 대장. 빵 함정·부채·해석 표적 포함 |
| ~11:00 | [eval/questions.yaml](eval/questions.yaml) — QA 26 + 행동 프로브 16 |
| ~11:15 | ⭐ **[experiments/cache_sim.py](experiments/cache_sim.py) — 첫 실험, 키 없이 실행됨** |
| ~11:20 | [11-experiment-results.md](docs/11-experiment-results.md) — 결과 + 정정 |

### ⭐ 실험 1에서 나온 것

1. **새 발견:** 최근 N턴 슬라이딩 윈도우는 append가 아니라 **evict**다. 1턴씩 미는 관행이 매 턴 캐시를 깨뜨린다. **청크 축출로 바꾸면 배치 순서 재정렬보다 큰 효과** (입력 47%↓ vs 29%↓)
2. **자기 교정:** 시뮬레이션이 내 K2 레이아웃 오류를 잡았다. *"의미 단위로 묶기"*는 빈도순이 아니다
3. **정정:** [07 A3](docs/07-assumptions-audit.md)에서 유저 간 캐시 공유를 *"명백한 누락"*이라고 강하게 썼는데, **세션 50턴 기준 이득이 0.6%**였다. **메커니즘의 존재와 크기를 구별하지 않았다.** 시뮬레이션 20줄이면 확인할 수 있었다
4. → [ADR-008](docs/adr/ADR-008-provider-model-tiering.md)의 롱테일 강등 정책 근거가 약해져 **재검토 표시**

### ⭐ 실험 2 (검색) — 11:40~12:10

[gen_corpus.py](experiments/gen_corpus.py)로 720턴 합성 코퍼스 생성 → [retrieval_sim.py](experiments/retrieval_sim.py)로 BM25 실험.

1. **한국어 토큰화:** 어절 40% vs 문자 bigram 60% (recall@5). **교착어 문제 실측 확인** ([FP-4](docs/03-architecture-modules.md))
2. **쓰기 정책이 검색보다 먼저다:** 원문 전부 색인 시 top-5의 **4~5개가 잡담**. 추출만 색인하면 **0/5**.
   → *"색인 안 한 것은 오주입될 수 없다"*([FP-8](docs/03-architecture-modules.md)) 확인. **후보가 쓰레기면 리랭커도 소용없다**
3. 🔴 **[ADR-005](docs/adr/ADR-005-forgetting-salience.md)의 스코어링 수식이 틀렸다.**
   importance 0.05짜리 트랩이 relevance 최댓값을 받으면 **곱해도(2위) 더해도(2위) 안 걸러진다.** 하드 필터만 작동(20위).
   → **현저성은 순위 조정이 아니라 자격 심사여야 한다.** Generative Agents의 3항 가중합을 그대로 가져온 게 실수였다
4. **자기 교정 2회:** 실험 2 초안이 노이즈까지 색인하는 오류 → 그게 오히려 쓰기 정책의 중요성을 드러냄. YAML 구조 오류도 파서가 잡음

**패턴:** 지금까지 실험 두 개가 **매번 내 문서의 오류를 하나씩 잡아냈다.** 시뮬레이션이 논증보다 싸다.

### ⭐ 실험 3 (arm 비교) — 12:10~12:35

[arms_sim.py](experiments/arms_sim.py) — 9개 arm을 동일 예산·동일 질문으로 비교.

1. 🔴 **검색 arm 간 비용 차이가 무의미하다.** A0 빼면 전부 $18.04~$20.22.
   고정 블록 1,370 + 출력 400이 지배해서 **주입 토큰 34 vs 1,039의 차이가 총액에서 안 보인다.**
   → **arm 선택은 비용이 아니라 품질 기준으로만 해야 한다.** [02 §1.3](docs/02-problem-definition.md)의 *"비용은 제약조건"*이 정량 확인됨
2. **세션 단위 청킹 recall 0%** — 청크가 커서 신호가 희석된다. [M2](docs/03-architecture-modules.md)의 S3 옵션 사실상 탈락
3. **F1 제안안이 C2(단순 추출 RAG)를 recall에서 못 이긴다** (둘 다 80%). 이긴 건 precision(38% vs 28%). *(이후 방해물 추가로 23%로 갱신)*
   → **복잡도가 값을 하는 지점은 "더 잘 찾는 것"이 아니라 "덜 잘못 넣는 것"이다.** 정직하게 표기함
4. **버그 발견:** `S21 turn 8`에 세 항목이 충돌해 하나만 렌더링됐다. Full-context가 100%가 아니라 80%로 나와서 잡았다.
   → **Oracle arm이 상한 검증 장치로 작동한 사례.** 100%가 안 나오면 데이터가 잘못된 것이다
5. ⚠️ **자기 비판:** 질문 10개가 전부 "오래된 걸 묻는" 유형이라 A2(Window-only)에 불공정하다.
   [08 D1](docs/08-decisions-under-uncertainty.md)의 *"15턴 이내 참조 75%"* 추정과 긴장 관계 — **질문 세트를 깊이 분포에 맞춰 가중해야 한다.** 문서에 명시함

### ⭐ 실험 4 (깊이별 교정) — 12:35~13:00

바로 위 5번의 자기 비판을 실제로 고쳤다. 같은 질문을 근거로부터 d턴 떨어진 시점에서 물어본다.

| arm | 얕음 ≤15 | 중간 ≤60 | 깊음 60+ | 가중 평균 |
|---|---|---|---|---|
| A2 Window-only | 100% | 0% | 0% | **75%** |
| C2 추출 RAG | 80% | 80% | 100% | **81%** |
| G1 현행 재현 | 100% | 80% | 83%* | **95%*** |
| F1 제안안 | 100% | 80% | 100% | **96%** |

1. 🔴 **MemDelta 재현.** 최근 15턴만으로 75%. 정교한 검색이 **6%p밖에 못 이긴다.**
   실험 3의 표는 실제 대화의 **5%만** 본 것이었다
2. 🔵 **Window와 Retrieval은 보완재다** (75% / 81% → 결합 96%). [08 D1](docs/08-decisions-under-uncertainty.md)의 4층 구조가 실측 지지됨
3. ⚠️ **F1이 G1을 recall에서 못 이긴다** (96% vs 95%). 이긴 건 precision(38% vs 26%)
   › *이후 실험 5에서 어휘 방해물을 추가하자 G1이 깊음 100%로 올라 **96% 완전 동률**이 됐다. 아래 참조*
   → **최종 추천의 성격이 바뀐다.** *"현행이 틀렸다"*가 아니라 *"현행은 잘 찾고 있고, 여지는 오주입과 비용에 있다"*
4. ⚠️ **[08 D1](docs/08-decisions-under-uncertainty.md)의 "분포에 강건하다"가 절반만 맞았다.**
   구조(4층)는 강건하지만 **"검색에 얼마나 투자할 가치가 있는가"는 분포에 크게 의존한다**

→ [10 최종 추천](docs/10-final-recommendation.md)에 §2.5 "실측이 말해준 것"을 추가해 확인/수정/깎임을 먼저 밝혔다.

---

## 자율로 내린 결정

### D-A. ADR을 10편으로 쪼갰다 (원래 계획 8편)

`03 §7`에서 계획한 8편은 [06](docs/06-data-model.md)~[09](docs/09-what-memory-is-for.md) 이전 구조였다. 이후 **메모리 범위 단위**(ADR-001)와 **프라이버시**(ADR-010)가 독립적 결정으로 커져서 분리했다.

*되돌리려면:* ADR-001을 ADR-002에, ADR-010을 ADR-002에 병합하면 8편이 된다.

### D-B. 낡은 문서를 덮어쓰지 않고 "갱신 표시"만 달았다 ⭐

[01](docs/01-problem-and-hypotheses.md)·[02](docs/02-problem-definition.md)·[03](docs/03-architecture-modules.md)·[05](docs/05-cost-latency-reality.md)에는 이후 문서가 뒤집은 내용이 남아 있다. 두 선택지가 있었다.

```
(a) 최신 결론으로 덮어쓴다     → 읽기 깔끔. 사고 과정이 사라짐
(b) 표시만 하고 원문은 남긴다   → 읽기 번거로움. 무엇을 왜 바꿨는지 보임
```

**(b)를 골랐다.** 무엇을 받아들이고 거절하고 고쳐 받아들였는지를 남기는 것이 이 검토의 목적이다. **덮어쓰면 그 흔적이 사라진다.**

*되돌리려면:* 각 문서 상단의 `> 🔄 갱신` 블록을 따라 본문을 수정하면 된다.

### D-C. 코퍼스보다 최종 추천을 먼저 썼다

원래 순서는 `ADR → 정합성 → 코퍼스 → 최종 추천`이었다. **최종 추천을 두 번째로 올렸다.**

이유: 원래 질문(*"처음부터 다시 설계한다면"*)에 대한 답이고, 이게 없으면 나머지 문서 20개가 결론 없는 조사가 된다. 그리고 컨텍스트가 가장 온전한 시점에 쓰는 게 품질이 좋다.

### D-D. 최종 추천에 `[결과 대기]`를 명시적으로 남겼다

실험을 못 돌렸으므로 정량 주장에 숫자가 없다. **빈칸을 채우지 않고 비어 있다고 표시**했다. 추정치를 넣으면 검증된 것처럼 읽힐 위험이 있다.

### D-E. README에 한계를 결론보다 먼저 배치했다

*"API 키 없이 작성했다", "현행 구조를 모른다", "조사가 불만 게시물 편의 표본이다"*를 상단에 뒀다. 약점으로 읽힐 수 있지만, **모르는 것을 먼저 밝히는 편이 결론을 믿게 한다**고 판단했다.

*동의하지 않으면:* README `⚠️ 이 검토의 한계` 절을 하단으로 옮기면 된다.

---

### ⭐ 실험 5 (하이브리드 검색) — 🔴 내 주장이 반증됨

`sentence-transformers` + `e5-small`을 설치해 [hybrid_sim.py](experiments/hybrid_sim.py) 실행.
프로브를 **어휘형/의역형으로 분리** — 한 종류만 보면 어느 쪽으로든 원하는 결론이 나오므로.

| | BM25 | **Dense** | Hybrid(RRF) |
|---|---|---|---|
| 어휘형(고유명사) 평균 순위 | 1.2 | **1.2** | 1.2 |
| 의역형 평균 순위 | 4.2 | **1.2** | 2.6 |

1. 🔴 **[FP-4](docs/03-architecture-modules.md)의 핵심 예측이 빗나갔다.** dense가 `나비`·`크로와상`·`카페인`을 전부 1~2위로 찾았다.
   *"어휘적 정확 매칭만이 지시체를 묶는다"*는 **너무 강한 주장**이었다
2. 🔴 **하이브리드가 dense 단독보다 나쁘다.** naive RRF가 BM25의 17위를 끌고 들어왔다
3. ⚠️ **동시에, 이 실험은 반증의 자격이 없다.** 검색 대상 22개에 어휘적 방해물이 없어 **실패 조건 자체가 없었다.**
   변명이 아니라 설계 결함이다 — **둘 다 정직하게 기록했다**
4. → [ADR-006](docs/adr/ADR-006-retrieval-gating-selection.md) *"하이브리드 필수"* → **"dense 우선, BM25 조건부"로 강등**

**[07 A3](docs/07-assumptions-audit.md)에서 배운 것과 같은 실수를 반복했다** — 메커니즘의 존재와 크기를 구별하지 않았다.

**그리고 결함을 실제로 고쳤다.** 대장에 어휘 방해물 5개를 심었다 — `나비(곤충)`, `나비넥타이`, `친구네 고양이 코코`, `라떼`, `스타트업 다니는 선배`. 같은 문자열이 다른 지시체를 갖게 만드는 게 목적이었다.

결과: **방향은 보이는데 크기가 작다.** Dense가 `나비` 질의에서 2위 → 3위로 밀렸고 BM25는 2위 유지. **평균 순위 차이 0.2.**
→ FP-4의 메커니즘은 실재하지만 **방해물 5개로 얻은 차이가 이 정도**다. 실제 코퍼스엔 수백 개가 있을 테니 벌어질 수도 있지만, **그건 지금 데이터로 뒷받침되지 않는 추측**이라 그렇게 표기했다.

**부수 효과 — 코퍼스가 커지자 다른 수치도 바뀌었다:**
G1(현행 재현)이 깊음 83% → 100%로 올라 **F1과 recall 완전 동률(96%)**이 됐다.
→ **제안안의 우위가 precision 하나로 좁혀졌다**(38% vs 22%). 전 문서에 반영.

### ⭐ 실험 6 (브루트포스 벡터 검색) — ✅ ADR-003 확인·강화

[vector_bench.py](experiments/vector_bench.py) — numpy 내적 실측.

```
256차원 int8   3,000개 → 0.13 ms / 0.73 MB      ← 우리 규모
              100만개 → 18.6 ms / 244 MB
Qdrant 네트워크 왕복(문헌값)  1~5 ms
```

1. ✅ **네트워크 왕복만으로도 브루트포스보다 10~40배 느리다.** [ADR-003](docs/adr/ADR-003-storage-engine.md) 성립
2. ⭐ **100만 벡터도 18.6ms** — [지연 예산 200~400ms](docs/05-cost-latency-reality.md) 안.
   **[06 §3.1](docs/06-data-model.md)의 카디널리티 추정이 300배 틀려도 결론이 안 바뀐다**
   → [08 §0](docs/08-decisions-under-uncertainty.md) 원칙 2번(*"추정이 틀려도 안 무너지는 설계"*)의 실제 사례
3. 차원 축소 실익: 1024 f32 11.7MB → 256 int8 0.73MB (**16배**)

### ⭐ 실험 7 (파라미터 스윕) — 내가 근거 없이 정한 값들

[param_sweep.py](experiments/param_sweep.py) — τ(하드 게이트), k(주입 개수), N(윈도우)을 스윕.

| 파라미터 | 내 추정 | 실측 | 판정 |
|---|---|---|---|
| τ | 0.2 | **0.2~0.4 어디든 평평**, 0.5부터 붕괴 | ✅ 안전한 선택 |
| N | 10~15턴 | **무릎 = 10턴** (5턴 81% → 10턴 96%) | ✅ 맞음 |
| k | 5 | **측정 불가** | ⚠️ 평가 설계 한계 |

1. **τ는 "최적값"보다 "둔감 구간"이 쓸모 있는 결론이다.** *"최적 0.3"*이라고 쓰면 과적합이다.
   파라미터가 둔감하다는 건 **튜닝에 시간을 안 써도 된다는 좋은 소식**이다
2. 🔴 **k=1이 최적으로 나왔는데, 그건 발견이 아니라 내 평가 설계의 산물이다.**
   질문마다 정답이 정확히 1개라 top-1만 넣으면 노이즈가 0이 되는 게 당연하다.
   → **이 결과로 k를 바꾸면 안 된다.** 스윕이 항상 답을 주지는 않는다
3. **첫 스윕 초안에 결함이 두 개 있었다** — k 아티팩트와 N이 토큰 비용을 무시한 것.
   예산 회계를 추가하고 k의 한계를 명시해서 고쳤다

**셋 중 둘은 추정이 맞았고, 하나는 "이 방법으로는 못 정한다"는 걸 알게 됐다.**
후자도 결과다 — **"모른다는 걸 아는 것"과 "잘못 정하는 것"은 다르다.**

### ⭐ 실험 8 (민감도) — 가장 하중이 큰 가정

[sensitivity.py](experiments/sensitivity.py) — 참조 깊이 분포를 6가지로 바꿔가며 각 arm의 가중 recall 계산.

| 분포 | Window-only | 제안안 | 검색 이득 |
|---|---|---|---|
| 95/4/1 | 95% | 99% | **4%p** |
| 90/8/2 | 90% | 98% | 8%p |
| **75/20/5 (내 추정)** | 75% | 96% | **21%p** |
| 60/25/15 | 60% | 95% | 35%p |
| 35/35/30 | 35% | 93% | **58%p** |

1. 🔴 **검색 이득이 14배 움직인다.** 손익분기는 **얕은 참조 약 85%**.
   → *"참조 깊이 히스토그램 하나가 투자 규모를 정한다"*가 숫자로 확인됨
2. ⭐ **내 추정(75%)이 정확히 애매한 자리(21%p)에 있다.**
   어느 쪽으로 밝혀지든 결정이 달라지므로 **이 측정의 가치가 최대**다
3. ✅ **그런데 precision 결론은 분포와 무관하다.** G1과 F1의 recall이 **모든 분포에서 동률**이다.
   → *"제안안의 가치는 precision"*이라는 결론이 **가장 불확실한 가정에 의존하지 않는다**
4. → **강건한 것과 아닌 것이 갈렸다:**
   4층 구조·precision 우위·캐시 최적화는 **분포 무관**(지금 해도 됨),
   검색 투자 규모는 **완전 의존**(측정 후 결정)

### ⭐ 실험 9 (상태 가드) — 반복 주장한 것을 코드로 검증

[guard_sim.py](experiments/guard_sim.py) — *"L2가 enum이면 정규식으로 잡힌다, NLI 불필요"*를 검증.
함정을 넣었다: **의미적 위반**(금지어 없이 상태 위반)과 **거짓 양성 함정**(인용·회상).

| 버전 | 정밀도 | 재현율 |
|---|---|---|
| 순수 정규식 | **50%** | 57% |
| **+ 억제 문맥** | **100%** | 57% |

1. **억제 문맥 처리가 결정적이다.** 순수 금지어 매칭은 FP 50% — *"사랑해 같은 말은 이제 안 할게"*를 위반으로 잡는다.
   **롤플레이는 자기 언급이 잦아서**(관계를 이야기하는 게 콘텐츠) 이 처리 없이는 못 쓴다
2. 🔴 **의미적 위반은 원리적으로 못 잡는다.** 재현율 57%에서 멈춘다.
   → **[05 §7.3](docs/05-cost-latency-reality.md)의 "NLI 대신 스키마 비교"는 과했다.**
   스키마 비교는 NLI를 **대체**하는 게 아니라 **필요한 양을 줄인다**
3. ⭐ **그런데 정밀도 100%라는 특성이 유용하다** — 울리면 반드시 맞으므로 재확인 없이 조치 가능
4. ⭐ **설계 통찰:** 의미적 위반은 **사후 검증보다 사전 주입**이 낫다.
   → [02 P4](docs/02-problem-definition.md)의 결정적 주입에 **두 번째 근거** — 회상뿐 아니라 **위반 예방**이기도 하다

### ⭐ 실험 10 (메커니즘 검증) — 설계만 하고 안 돌려본 것들

[mechanism_sim.py](experiments/mechanism_sim.py) — L9 부채 트리거, L6 `surfaced_count`, M 커버리지를 실제로 돌렸다.

1. 🔴 **L9에 백오프가 없다.** `session_start` 트리거가 설정 후 회수까지 **매 세션 발화**한다.
   캐릭터가 4세션 연속 같은 걸 묻는다 = 집착. `status`만으로는 *"시도했으나 회수 안 됨"*을 못 적는다
2. 🔴 **트리거의 시계가 모호하다.** `3d`는 벽시계인데 세션은 날짜와 1:1이 아니다.
   하루 세 세션인 유저도 있다. 명시가 없어 실험에서 **아예 발화하지 않았다**
3. → 스키마에 `last_attempted_at`·`attempt_count`·`trigger_clock` **3개 필드 추가.**
   **종이에 그릴 땐 안 보였고 20줄 시뮬레이션에서 바로 나왔다**
4. **surfaced 페널티 무릎 = 0.1** (재주입 89% → 11%, relevance 손실 1%)
5. 🔵 **더 중요한 발견: 모호한 발화엔 검색 신호가 없다.**
   *"요즘 어때"* 같은 잡담에서 평균 relevance가 0.10이다. **무엇을 꺼내든 우연이다.**
   → [ADR-006](docs/adr/ADR-006-retrieval-gating-selection.md) 게이팅의 본래 목적이 "비용 절감"이 아니라
   **"신호 없는 턴에서 우연을 주입하지 않기"**임이 드러났다
6. ✅ **커버리지 5/5** — 룰 판정이라 결정적

### 정합성 감사 — 실험 후

정정을 10번 넘게 하다 보니 **고쳤다고 생각한 숫자가 다른 곳에 남아** 있었다.

- 낡은 수치 **3곳 발견·수정** (`96% vs 95%` → 동률, `38% vs 26%` → `22%`, `결합 95%` → `96%`)
- → ⭐ **[run_all.py](experiments/run_all.py)** 추가. 전 실험을 돌려 [RESULTS.txt](experiments/RESULTS.txt)에 모은다.
  **수치의 단일 출처.** 문서에 숫자를 옮겨 적는 한 이런 어긋남은 반복되므로, 대조할 곳을 만들었다
- 링크 28개 md 전부 정상, 실험 10개 전부 클린 실행 재확인

### 신뢰 수준 명시

정정을 많이 기록하다 보니 **보고서가 자기부정처럼 읽힐 위험**이 있었다.
확신할 수 있는 것이 확신 있게 전달되도록 한 장 요약본에 🟢/🟡/🔴 분리를 추가했다.

- 🟢 **측정으로 뒷받침** — 캐시·배치, 벡터DB 불필요, 쓰기 정책, 형태소 필수, 하드 게이트
- 🟡 **논리·시장 증거는 있으나 품질 미검증** — 상태/사건 이원화, 서사 부채, 해석 우위, PPR 목적함수
- 🔴 **추정** — 참조 깊이 분포, 턴 수·응답 길이, 현행 구조 재구성

**핵심: 🟢은 지금 실행해도 되고 🔴은 측정 후에 정하면 된다. 그리고 둘은 독립적이다.**

### ADR 근거 감사

[docs/adr/README.md](docs/adr/README.md) — ADR 10개가 각각 무엇으로 뒷받침되는지 분리했다.

```
🟢 실측 4  ·  🟡 부분/산술 4  ·  ⚪ 자기참조 1  ·  🔴 법무 필요 1
```

**10개 중 4개만 측정으로 뒷받침된다.** 그리고 **그 4개가 전부 "지금 바로 실행 가능한" 항목**이다 —
측정 가능한 것은 대체로 값싸게 확인되고, 값싸게 확인되는 것은 대체로 값싸게 고칠 수 있다.

그리고 [ADR 템플릿](docs/adr/ADR-000-template.md)의 **"재검토 트리거"** 항목이 실제로 작동했다.
실험이 돌 때마다 트리거가 걸렸고, 그래서 **뒤집힌 ADR 5건이 조용히 묻히지 않았다.**

### 🔴 사고 — 재현성 검증이 수기 세션을 지웠다

클린 상태 검증으로 `rm -rf eval/corpus`를 돌렸다. **수기 세션 5개가 `eval/corpus/handwritten/`에 있었다.**
재생성 스크립트가 지우는 범위 안에 **재생성 불가능한 것**을 넣어둔 것이다.

전부 다시 썼고, 이번엔 `eval/sessions/`(코퍼스의 형제 디렉터리)에 뒀다.

> **그런데 이게 웃긴 지점이 있다.** [06 FP-7](docs/06-data-model.md)에서 *"원본은 지우지 않는다"*를
> 길게 논증해놓고, **정작 제 파일 배치가 그 원칙을 위반하고 있었습니다.**
> 원칙을 문서에 쓰는 것과 그 원칙대로 배치하는 것은 다른 일입니다 — [13 발견 1](docs/13-prototype.md)과 같은 종류의 실패입니다.

---

### ⭐⭐ 실험 11 (소크 테스트) — 이 구간에서 가장 큰 발견

**왜 했나.** 세 자산이 서로 만난 적이 없다는 걸 알아챘다.
```
experiments/*     순수 시뮬레이션.  SQLite도 실제 읽기 경로도 안 탄다
prototype/*demo   실제 구현.        항목 3~5개짜리 손수 만든 시드
eval/corpus       720턴 코퍼스.     구현에 넣어본 적 없음
```
각각은 검증됐는데 **연결부는 아무도 안 봤다.** 붙였다 → [prototype/soak.py](prototype/soak.py)

**나온 것 (전부 재현 가능: `python prototype/soak.py`)**

| 설정 | 근거 도달 | 검색 순증 | 트랩 | p50 토큰 |
|---|---|---|---|---|
| A. 검색만 (원래 구현) | 4/18 = **22%** | 4 | 없음 | 490 |
| B. + `[알고 있는 것]` 전량 | 10/18 = **56%** | 0 | **주입됨** 🔴 | 799 |
| C. + 상시성 필터 (최종) | 9/18 = **50%** | 0 | 없음 | 689 |

**① 설계 문서에 있는 블록을 구현이 통째로 빼먹었다.** [15](docs/15-read-path-prompt.md)에서 `[알고 있는 것]`/`[꺼낼 만한 것]`으로
나눠 설계해놓고 `build_context`는 후자만 구현했다. `facts_at()`은 `fact_demo`에서만 불려서 **구멍이 안 보였다.**

**② 그 수정이 하드 게이트를 우회했다.** 트랩(중요도 0.05)이 평범한 턴에 들어간다 — 유사 서비스 A의 *"빵 유언"*을 제 손으로 재현.
방어선을 **검색 경로에만** 세웠기 때문이다.

**③ importance로는 못 거른다.** 술어에 **상시성(standing)**이 필요했다. *중요한가*가 아니라 **속성인가 사건인가**.
카디널리티·가변성(발견 4)에 이어 **세 번째로 구현이 강제한 술어 속성**이다.

**④ 부수 — 쓰기 지연은 전부 fsync였다.** 5.192ms → 묶으면 0.001ms(**4,000배**).
질문이 *"쓰기를 빠르게"*가 아니라 **"무엇이 내구성을 필요로 하나"**로 바뀐다.
L0 원본은 즉시 커밋, 파생물은 [06 FP-7](docs/06-data-model.md)에 따라 재생성 가능하므로 묶어서 커밋.

> ### 그리고 이게 실험 1~10의 해석을 바꿨다
> 1~10은 전부 **부품을 격리해서** 쟀다. 검색기만 재면 `recall@5 = 70%`인데 파이프라인 전체는 **22%**다.
> **부품 벤치마크는 시스템 성능의 상한이지 예측치가 아니다.**
>
> 반영: [docs/10 §2.5](docs/10-final-recommendation.md)에 정정 블록, [§5 도입 순서](docs/10-final-recommendation.md) 2단계 승격,
> 한 장 요약본·[README](README.md) 갱신, [docs/06](docs/06-data-model.md)·[14](docs/14-extraction-prompts.md)에 standing 추가.

⚠️ **한계를 같이 적는다.** 문항 18개(1건 = 5.6%p) · 추출기가 이상적이라고 가정(L1 실패 0) ·
응답을 생성하지 않으므로 도달률은 **L3 활용의 상한**. **검색 순증 0을 "검색 불필요"로 읽으면 안 된다.**

---

### ⭐⭐ 실험 12 (게이트·임계 스윕) — 제 결론을 두 번 정정

실험 11이 *"검색 순증 0"*과 *"26문항 중 16개가 게이트에 막힘"*을 같이 냈다.
붙이면 가설이 나온다 — **검색이 무능한 게 아니라 게이트가 목을 조른 것 아닌가?** 재봤다.

**1부 — 가설 기각.** 게이트를 완전히 열어도 **회상이 1건도 안 올랐다.** 호출과 잡음만 늘었다.

**2부 — 진짜 범인은 θ.** 정답 근거의 질문↔요약 bigram 중첩이 대부분 **0.00~0.11**인데 **θ = 0.15**.
**정답이 임계 아래에 깔려 있었다.**

| θ | k | 회상 | 검색 순증 | 오주입 |
|---|---|---|---|---|
| 0.15 | 5 | 9/18 | 0 | 39 |
| **0.05** | **5** | **11/18** | **1** | **62** |
| 0.00 | 5 | 13/18 | 4 | 439 |
| 0.00 | 20 | 17/18 | 6 | 3052 |

회상 1건당 오주입이 12 → 188로 급증하는 지점이 **θ=0.05**. precision이 비싼 도메인이라 여기서 멈춘다.
→ `THETA_RELEVANCE` **0.15 → 0.05** 반영.

**3부 — ⭐ 왜 실험 7이 못 봤나: 상호작용**

```
게이트만 열면    9 → 9    (효과 0)
θ만 낮추면       9 → 9    (효과 0)
둘 다 바꾸면     9 → 11   (+2)
```
두 방어선이 **직렬**이라 하나만 열면 다른 하나가 막는다. **주효과가 둘 다 0인데 교호작용만 있다.**
**한 번에 하나씩 바꾸는 스윕(OFAT)으로는 구조적으로 발견 불가능하다.** 실험 7이 정확히 그 방식이었다.

> **[13 발견 3](docs/13-prototype.md)에서 *"직렬 방어선은 앞의 것이 막으면 뒤의 것이 검증되지 않는다"*고
> 적어놓고, 같은 함정에 파라미터 튜닝에서 다시 빠졌습니다.**
> 첫 번째는 결과 해석의 문제였고, **두 번째는 측정 설계 자체의 문제**였습니다.
>
> 그리고 이건 처음 지시하신 *"모듈 조합을 테스트해서 최적 구조를 찾으라"*가 왜 옳았는지의 사례이기도 합니다 —
> **조합을 안 보면 안 보이는 것이 실제로 있었습니다.**

**4부 — 조합 격자.** 상호작용이 확인됐으니 게이트 4종 × θ 2종을 전부 돌렸다.

| 게이트 | θ=0.15 | θ=0.05 | 검색 호출 | 오주입(θ=0.05) |
|---|---|---|---|---|
| G0 현행 (과거참조) | 9/18 | 9/18 | 19/436 | 21 |
| G1 게이트 없음 | 9/18 | 11/18 | 436/436 | 62 |
| G2 의례적만 차단 | 9/18 | 11/18 | 336/436 | 62 |
| **G3 내용어 접점** | 9/18 | **11/18** | **95/436** | **52** |

→ **권고: G3 + θ=0.05.** 회상 최대이면서 호출 78% 절감을 유지하고 오주입도 G1보다 적다.
`memory.py`의 `gate()`와 `THETA_RELEVANCE`에 반영했다.
**파이프라인 전체 근거 도달률 22% → 61%.**

> G3가 값싼 이유는 신호를 *"질문의 형태"*가 아니라 **"저장소와의 접점"**에서 찾기 때문입니다 —
> 무엇이 저장돼 있는지는 이미 알고 있으니 추가 비용이 거의 없습니다.

**그리고 재현성 규칙을 하나 세웠습니다.** 파라미터를 바꾸자 소크의 A~C가 다른 숫자를 냈다 —
**발견 자체가 재현되지 않는 상태**가 됐다. 그래서 소크 A~C와 게이트 스윕 1부는
**발견 당시 값으로 고정**하고, 현재 상태는 별도 행(소크 D)으로 보여준다.
*"진단은 진단 당시 조건에서 재현돼야 한다"* — 안 그러면 왜 그렇게 고쳤는지가 사라진다.

⚠️ 한계: 문항 18개 · 오주입을 *"대장에 없는 것"*으로 근사 · 검색기는 bigram 고정
(형태소 분석기를 쓰면 θ 문제의 크기 자체가 달라진다) · **θ=0.05는 이 코퍼스의 무릎이지 보편 상수가 아니다.**

---

### 🟢 실험 13 (응답 품질) — 비어 있던 5번째 축을 채웠다

사용자가 *"제미나이는 무료 API키가 있지 않냐"*고 지적. 맞았다. 그게 이 검토 최대 한계를 풀었다.

**무엇이 무료로 되는지 가른 것이 설계의 핵심이었다.** [eval/README §7](eval/README.md)의 Step 0(L3 측정)은
정답이 문자열이라 **LLM 심판이 필요 없다.** 반면 행동 프로브는 심판이 필요한데 Gemini뿐이라
자기선호 편향이 생긴다 → **편향된 숫자를 내느니 안 돌렸다.**

| arm | 정답 |
|---|---|
| **제안안** | **73%** |
| **현행 재현(G1)** | **65%** |
| Oracle (정답 근거만) | 65% |
| 최근 30턴 | 23% |
| 기억없음+규칙만 | 15% |
| 기억 없음 | 12% |

```
L3 활용 실패 35%  >  L2 검색 실패 8%
```
**Step 0의 답:** 메모리 구조를 완벽히 고쳐도 상한이 낮다 → 배치·프롬프트가 먼저.
다만 **메모리는 품질을 6배 올린다**(12%→73%). 두 작업은 경쟁하지 않는다.

**제안안 73% vs 현행 65%.** 차이 2문항은 잡음 범위지만, **제안안만 맞힌 3문항이 전부
사실 회상·귀속**(여동생·직업·화자)이라 `[알고 있는 것]` 결정적 주입 영역과 정확히 겹친다.
현행은 *"누가 스타트업 얘기를 했나"*에서 **"내가 했었지"로 화자를 뒤바꿨다.**

**🔴 그리고 "Oracle = 상한"이라는 제 이름이 틀렸다.** 제안안이 Oracle을 넘었다.
Oracle엔 정답 근거만 있고 요약·상시 사실·관계 상태가 없어 abstention에서 지어냈다.
→ **Oracle은 L3를 재는 데는 유효하지만 성능 상한이 아니다.**

**🔵 가설 하나가 반박됐다.** A0(0/6)와 A2(6/6)의 차이가 규칙 블록 때문이라고 봤는데,
규칙만 주니 **2/6**에 그쳤다(제안안 4/6). **환각 방어는 프롬프트가 아니라 기억에서 온다.**
프롬프트 한 줄로 싸게 해결되길 바랐는데 아니었다.

**🔴 G1 도입 시 교란을 만들었다.** G1에만 `[장기 요약]`을 주고 제안안엔 안 줬는데,
그 요약이 6문항의 답을 직접 담고 있었다. 제안안에 같은 요약을 주고 **재실행**했다.
같은 arm이 54% → 73%로 움직였다 — **조건이 숫자를 19%p 바꾼다.**

**🔵 예상 못 한 것 — 제안안이 Oracle을 3번 이겼다.** 전부 abstention이다.
Oracle은 정답 근거만 줘서 *"없음"*인 문항에서 지어냈고(*"당연히 알지, 초코잖아"*),
제안안은 `[알고 있는 것]`이 통째로 있어 부정할 수 있었다(*"너 강아지 안 키우잖아"*).
→ **모른다고 말하려면 무엇을 아는지를 알아야 한다.** 부정 지식은 **경계 정보**에서 나온다.

**🔴 숫자를 내기 전에 아티팩트 3개를 걷어냈다.**
① 응답이 12자에서 잘림(Gemini 3.x는 사고 토큰이 출력 예산을 먹는다) → 그대로면 L3 50%
② `세 번`≠`3번` → Oracle이 맞힌 걸 오답 처리해 **상한이 깎임**
③ abstention 정규식이 양방향으로 틀림 → 지어낸 걸 정답, 올바른 정정을 오답

> **첫 자동 채점이 Oracle 19% < 제안안 31%였다. 상한이 그 아래일 수 없다는 불가능성이 단서였다.**

**🔴 그리고 자동 채점만으로는 애초에 불가능했다.** gold 상당수가 문자열이 아니라 **채점 기준**이다
(*"이직 전후로 달라진 점을 짚음"*). 제가 그렇게 설계해놓고 자동 채점만으로 숫자를 내려 했다.
→ 수기 판정을 문항별 근거와 함께 [QUALITY_ADJUDICATION.yaml](experiments/QUALITY_ADJUDICATION.yaml)에 남겼다.
**판정자가 설계자와 같아** 유리하게 볼 유인이 있으므로, 반박 가능하게 두는 것이 최선이었다.

**보안 관련 판단 하나.** 처음에 `$env:GEMINI_API_KEY="..."`를 **제가 실행하는 셸** 기준으로 안내했는데,
그러면 **키가 대화 기록에 평문으로 남는다.** 사용자가 지적해서 정정했고,
본인 터미널 실행을 권하되 `.gemini_key` 파일 경로도 열어뒀다(스크립트는 키를 출력하지 않는다).

⚠️ 남은 것: **A0 arm에 규칙 블록이 빠진 설계 결함**(환각 원인을 못 가름), **G1 현행 재현 미실행**,
행동 프로브 18개 미측정, 26문항·1회 실행.

---

### ⭐ 설계 요소 점검 (16) — 실험 모드에서 나오다

사용자 지적: *"실험만 끝내면 다야?"* 맞는 지적이었다. **실험 실행 모드에 갇혀 있었다.**
원래 질문의 마지막 물음은 *"처음부터 다시 설계한다면"*이고, 그건 **설계 요소를 빠짐없이 짚는 일**이다.

기억 시스템을 **정보 생애주기**로 잘라 요소를 전부 나열했더니 **여섯 개가 비어 있었다.**
전부 실험으로는 안 나왔을 것들이다 — **목록을 만들어 훑어야만** 나온다.

| 요소 | 결과 |
|---|---|
| **콜드 스타트** | 🔴 실측 **첫 세션 환각 83%** → 프롬프트 3줄로 **17%** |
| 점진적 저하 | 🟢 구현. 계층별 실패 정책 (사실 계층만 "조용한 환각") |
| 삭제 전파 | 🟢 구현. `derivation`·`stale` |
| 중복 제거 | 🟢 구현. 시점 버킷 + 어근 겹침 |
| 스키마 진화 | 🟢 구현. 버전을 데이터에 기록 |
| 프롬프트 주입 | 🟡 **논증은 컸는데 실측 0/45.** 위험 평가가 과장이었다 |

**🔴 그리고 실험 범위가 결론 범위였다.** 모든 실험이 720턴에서 끝나는데 원래 질문은 *"아주 오랫동안"*이다.
[06 L4](docs/06-data-model.md)의 *"전체 재작성"*이 **입력을 안 정한 채로** 있었고,
400세션에서 $376 vs $0.49(760배)가 갈린다. 24세션에서는 안 보였다.
→ **사슬 깊이가 핵심**이라 "모든 세션 요약에서 재작성"하면 드리프트 유계 + 비용 1/40을 동시에 얻는다.

**🔴 교착어 문제가 세 번째로 물렸다** (검색 → 채점 → 중복제거).
매번 그 자리에서 정규식을 하나씩 붙였다. [FP-4](docs/03-architecture-modules.md)에서
*"형태소 분석 필수"*라 결론내고 **인프라로 안 둔** 결과다.

**🔴 그리고 "모른다고 했나 vs 지어냈나" 판정을 정규식으로 하려다 세 번 틀렸다**
(실험 13 · 16 · 17). 세 번 같은 곳에서 틀렸으면 **방법의 한계**다 —
이런 판정은 처음부터 사람이나 다른 벤더 심판에게 넘겨야 한다.

> **방법론적으로:** 이번엔 순서가 반대로도 작동했다.
> 목록에서 위협을 **상상하고**(주입) 방어를 설계한 뒤 **실측이 크기를 깎았고**(0/45),
> 반대로 콜드 스타트는 상상이 위험을 **과소평가**했고 실측이 83%로 올렸다.
> **어느 쪽이 클지는 미리 알 수 없다. 그래서 둘 다 해야 한다.**

---

### 설계 요소를 코드로 — ADR 011~013 추가

[16](docs/16-design-elements.md)에서 찾은 빈칸들이 **결정 기록으로 승격**됐다.
프로토타입에 구현하고 실측한 것들이라 001~010과 달리 **채택** 상태다.

| ADR | 근거 |
|---|---|
| [011 파생물 전파](docs/adr/ADR-011-derivation-propagation.md) | 🟢 실측 — stale 주입 **23턴 → 0** |
| [012 콜드 스타트](docs/adr/ADR-012-cold-start.md) | 🟢 실측 — 첫 세션 환각 **83% → 17%** |
| [013 요약 재작성 입력](docs/adr/ADR-013-summary-regeneration.md) | 🟡 계산 — 400세션 **$376 → $9.4** |

> **001~010은 설계를 먼저 하고 실험으로 검증**했고,
> **011~013은 목록을 훑다가 빈칸으로 발견**된 것들이다. 경로가 다르다.

### 🔵 그리고 결론은 같은데 이유가 바뀐 사례

`stage_note`(자유 텍스트)를 결정적 주입에서 빼기로 한 결정은 **보안 논증**에서 나왔다
(자유 텍스트 × 매 턴 주입 = 최대 공격 표면).
그런데 [실험 16](docs/11-experiment-results.md)에서 주입 공격이 **0/45**로 그 근거가 약해졌다.

**대신 [실험 18](docs/11-experiment-results.md)에서 더 단단한 근거가 나왔다** —
결정적 주입 블록이 436턴 내내 **한 가지 모양**이라 접두사가 안 깨진다.
`stage_note`를 넣으면 대화마다 바뀌므로 **그 성질이 사라진다.**

> 결론은 그대로인데 **이유가 바뀌었고, 바뀐 이유가 더 낫다.**
> 논증으로 시작해 실측이 그걸 깎았지만, 그 과정에서 다른 근거를 찾았다.

---

### 실험 18~19 — 지표를 바꾸자 진짜 버그가 둘 나왔다

**18. 퀴즈(n=26) 대신 턴(n=436)을 셌다.** [실험 14](docs/11-experiment-results.md)의 검정력 계산이
*"26문항으로는 못 가른다"*고 했으니 지표를 바꿨다. 표본이 17배가 된다.

→ **stale fact가 23턴에 주입되고 있었다.** `fact` 행은 무효화됐는데
**검색용 색인 복사본은 그대로 남아** 계속 검색됐다.
[16 §4](docs/16-design-elements.md)에서 삭제 전파를 만들며 **'갱신'을 빠뜨린** 것이다.
둘 다 *"원본이 더 이상 그것이 아니다"*인데 다른 일로 봤다. → **23 → 0.**

> `facts_at`을 **단위로만** 확인했지 436턴 재생으로 확인한 적이 없어서 못 봤다.
> 실험 11 소크의 교훈이 반복된다 — **부품 검증과 시스템 검증은 다른 것을 잡는다.**

⚠️ 그리고 그 23이라는 숫자도 처음엔 틀렸다. 컨텍스트 **전체**에서 찾아서
원본 로그의 유저 발화까지 셌다. **기억 블록만** 세도록 고쳤다.
지표를 만들 때 *"무엇을 세지 않을지"*가 무엇을 셀지만큼 중요하다.

**19. 요약이 사실을 얼마나 보존하는지 쟀다.** ADR-013에 *"드리프트 크기를 안 쟀다"*고
적어뒀으니 쟀고, **내 논증이 기각됐다** — 사슬 깊이 2와 12가 똑같이 1/11이었다.

**대신 더 큰 게 나왔다.**
```
원본 11/11  →  세션 요약 10/11  →  lifetime 1/11
```
원인을 직접 시험하니 **예산을 3배 늘려도 2→2**인데
*"구체적 사실을 먼저 적어라"*를 넣으니 **2→7**이었다.

> **범인은 제 프롬프트였다.** [14 P4](docs/14-extraction-prompts.md)에
> *"감정선과 디테일을 살려라. 사건 나열이 아니다"*라고 써놨다. **모델은 시킨 대로 했다.**

그런데 **프롬프트를 고치는 게 답이 아니다.** 요약의 일은 타임라인·관계고
사실은 L5의 일이다. 지시를 고쳐도 7/11로 세션 요약(10/11)에 못 미쳤다.
→ **분업을 명시**하는 한 줄을 P4에 넣었다.

> ⭐ 그리고 [실험 13](docs/11-experiment-results.md)에서 **제안안이 현행을 이긴 3문항이
> 전부 사실 회상**이었다. 현행은 요약+검색에 의존했고 요약은 그 사실을 안 담고 있었다.
> **별개로 설계한 두 실험이 같은 곳을 가리킨다.** 이 검토에서 가장 강한 수렴이다.

### 🔧 도구 하나 — 문서 숫자 정합성 감사

문서 45편에 실험 20개를 반영하다 보니 같은 숫자가 여러 곳에 인용된다.
링크 검사는 하고 있었는데 **숫자 검사는 없었다.**
돌려보니 **실험 개수가 문서마다 18/17/15/10, ADR이 13/10/6**이었다.

⚠️ `PROGRESS.md`와 초기 계획서는 **시간순 기록물이라 옛 값이 정상**이다. 제외했다 —
안 빼면 감사가 정상인 것을 계속 빨갛게 만들고, **그러면 아무도 안 본다.**

---

### 마무리 — 13:00~13:15

- [08 D1](docs/08-decisions-under-uncertainty.md) *"분포에 강건하다"* 정정 — **구조는 강건하지만 투자 규모는 아니다**
- 재현성 검증: 코퍼스 삭제 후 4개 스크립트 전부 클린 실행 확인
- [experiments/README.md](experiments/README.md) — 실행 방법 + 결과 요약 + 한계
- ⭐ **한 장 요약본** 추가(이 저장소에는 올리지 않음).
  문서 총량이 370K라 *"간단하게 정리해서 공유"*라는 요청에 안 맞았다. 한 번에 읽을 수 있는 분량이 필요했다

---

### 🔄 2026-09-09 — 1차 정리 이후: 계측기 수리와 관계 FSM (단계 0~2)

세 모듈(관계 FSM · 임베딩 검색 · 요약)을 붙이려다 **계획이 읽으려던 지표 두 개가 고장난 것**을 먼저 발견해서, 순서를 바꿔 자부터 고쳤다.

**단계 0 — 계측기 수리**
- 🔴 **실험 12의 "오주입 49건"은 인공물이었다.** `gate_sweep.py`가 정답 근거를 `object` 한 문자열로만 봤고, 검색 경로는 `text`로 색인한다. `soak.py`의 `qa_eval`은 이미 옳게 읽고 있었다 — **두 곳이 같은 대장을 다르게 읽고 있었다.** 고치면 격자 **8셀 전부 0**이고, 그 0은 개선이 아니라 **지표가 아무것도 못 잰다는 증거**다. 사양을 폐기하고 `experiments/precision.py`(질문별 정밀도)로 대체 — 4셀 `2 / 4 / 3 / 11`
- 생존 지표를 `survived_frozen`(옛 값 비교 전용) / `survived_v2`(1글자 어근 금지·편재 어근 제외)로 이원화. 옛 숫자를 지우지 않고 나란히 둔다
- `apply_meta`에 **컬럼 화이트리스트** — 컬럼명이 SQL로 보간되고 있었고, `chat_id`를 델타로 받는 건 주입이 아니라 **테넌시 위반**이었다
- 정합성 감사가 `.omc/` 계획 문서를 읽어 실패하던 것 수정 (대상 80편 → 44편, 종료 1 → 0)

**단계 1 — 배선만** `prototype/embedding.py`(bge-m3, 실패 시 `None` + SKIP(77)) · `prototype/llm.py`(`temperature=0`, 실패 시 예외) · 스키마 **v4**(측면 테이블 4개, 기존 14개 테이블은 컬럼 하나도 안 건드림). 검색 동작은 한 줄도 안 바꿨다 — 출력이 바이트 동일하다

**단계 2 — 관계 FSM** [ADR-014](docs/adr/ADR-014-relationship-state-machine.md) · [실험 20](docs/11-experiment-results.md)
- `prototype/fsm.py` — stage enum·전이표·0~100·stage별 밴드·한 턴 ±5. 위반은 거부 + `state_violation`에 사유별 기록
- 실측: 불법 주입 20종 **20/20 차단**(20행), 대장 궤적 5전이 **5/5 통과**, 전이마다 파생 요약 stale (ADR-011이 `fact` 경로에만 붙였던 전파를 `relationship` 전이로 확장)
- 🔴 **규칙이 "금지"가 아니라 "선언된 예외"로 약해졌다** — 대장 5전이 전부가 예외 선언으로 통과한다. 그리고 예외의 자기 선언을 확인하려던 동시발생률 5/5는 **기저율이 100%**라 사실상 무정보다
- 🔴 밴드가 델타 조건 안에만 있어서 **stage만 담긴 델타가 검증기 자신이 거부할 상태를 쓸 수 있었다.** 결과 상태로 다시 보도록 고치고 테스트를 넣었다(숫자는 한 칸도 안 움직였다)

**⏸️ 단계 3~5는 보류.** 임베딩 검색 전환·요약 프로토타입·stale 서빙 정책. θ가 척도 종속이라(`median(어휘)=0.000 < θ=0.05 < min(코사인)=0.241`) 전환은 격자를 다시 돌린 뒤에 한다.

기준선 대비 이동: `soak`의 DB 크기 한 줄(빈 테이블 4개의 페이지 비용 36KB)과 감사 대상 편수뿐. **회상·정밀도·순위·건수는 한 줄도 안 움직였다.**

### 🔄 2026-09-09 — 라운드 2 (R2a): 자를 한 번 더 재고, 임베딩 전환을 **예산에서** 멈췄다

계획은 검색 모드 격자(30셀)까지 가는 것이었다. 격자 앞에 **지연 게이트**를 하나 두었고 **그 게이트가 실패해서 격자를 돌리지 않았다.**
이 라운드에 **제품은 한 줄도 바뀌지 않았다** — `memory.py`의 기본값 전부 불변. 바뀐 것은 계측기와 문서다.

**단계 0 · 0' — 라벨 수리 (값 불변)**
- 🔴 **기준셀이 프로덕션이 아니었다.** `Memory.gate`(`memory.py:1002`)는 내용어 접점 게이트(G3)인데 `precision.py`는 `G0 · θ=0.05`를 원점이라 부르고 있었다. 462개 발화에서 `Memory.gate`와 G3의 판정 불일치 **0건**. 프로덕션은 `|검색≥1건|` **10/18**이고 G0가 3/18이다 — *"18문항 중 3문항만 검색한다"*는 **G0의 성질**이었다
- 🔴 **감사기의 CLAIM 하나가 틀린 숫자를 동결하고 있었다**(오주입 4 → **11**). 정합성 감사는 **정확성이 아니라 일관성**을 보장한다는 것이 이 사례로 확인됐다. CLAIM 이름에 실행 구성 `(G3·θ=0.05)`을 박아 다음에는 감사기가 먼저 울게 했다
- 라벨과 조회 키를 **영구히 분리**했다 (적대적 개명 시험으로 확인 — 라벨을 아무 문자열로 바꿔도 지표와 종료 코드가 안 움직인다)
- 동시발생률 5/5를 **±k 사다리 + 기저율 병기**로 교체: `±5 20%/29.9% · ±10 40%/52.0% · ±20 80%/81.5% · ±30 100%/92.3%`. **전이가 5개라 한 전이가 20%p** — 정직한 결론은 *"이 표본에서는 사다리가 질문을 해소할 수 없다"*이다

**단계 1 · 2 — 지표를 쌍으로, θ를 규칙으로**
- 정밀도를 **항목 단위 풀링**으로 바꾸고 **근거 회상**을 쌍으로 붙였다. 프로덕션 셀 `pooled 0.500 (22항목)` · `회상 11/28` · `top1 4/10`. **macro는 프로덕션 셀에서도 8문항이 검색 0건이라 1.0을 공짜로 받아 0.741**이고, 가장 적게 꺼내는 셀이 0.944로 가장 높다 — 매크로는 쓰지 않는다
- `hard_misinjection` **은퇴**: 4셀 × 5문항 20개 값 전부 0이고 τ=0 · θ=0 · 게이트 전개 · `TOP_K=1`에서도 0이다. 원인은 τ가 아니라 **distractor가 근거를 이길 수 없다**는 것 — 파라미터로 도달 불가다. 회귀 탐지기로만 남긴다
- ⚠️ `top1 4/10`은 **rowid 위에 서 있다** (Q05의 1·2·3위가 `s=0.4` 동점, 정렬 키가 점수 하나뿐). 뒤집히면 5/10. 나머지 세 셀은 동점 0건
- 실제 검색 모집단 **378쌍**(문항 18 × 색인 21행)에서 θ 분포를 쟀다: 어휘는 **0의 원자 90.7%**라 `θ=0.05`가 `θ=0.0001`과 똑같이 35쌍을 통과시키고, 코사인은 **최솟값 0.2142**로 통과율 100%다. **같은 숫자가 두 척도에서 반대 이유로 무력하다**

**🔴 지연 게이트 — FAIL. 그래서 격자를 안 돌렸다**
- bge-m3 쿼리 임베딩 1회 = **웜 p50 2,082 ms**(콜드 2,111 · ollama 0.33.3 · 이 기기). `docs/05` TTFT 예산 200~400 ms의 **5.2배**
- **20건 배치가 건당 2,074 ms로 개별 2,068 ms와 같다 — 배치 이득 1.0배.** 비용이 호출 오버헤드가 아니라 **연산**이고, 프로덕션의 TTFT를 정하는 것은 **쿼리 1건**이라 배칠 대상도 없다
- 그리고 `memory.py`는 `embedding`을 **import조차 안 한다** — 벡터 저장·조회 호출부 **0건**. 이 상태로 켜면 조회당 ≈45.8초다
- → 검색 모드 구현 · 30셀 격자 · 승격 판정을 **미실행**으로 두고 [ADR-015](docs/adr/ADR-015-retrieval-modes.md)를 **음성 결과 문서**로 마감했다. **격자 설계는 사전 등록된 채 보존한다** — 지우면 다음 사람이 결과를 보고 다시 설계하게 된다
- 재개 조건은 관측 가능한 한 줄: **같은 절차를 그대로 반복해서 웜 p50 ≤ 400 ms**

> 🔄 **위 `지연 게이트 — FAIL` 절의 수치와 판정은 번복됐다** — 아래 *"F38 번복"* 항목을 볼 것. **이 절은 그때 무엇이 적혔는지의 기록이므로 고치지 않는다**(PROGRESS.md는 타임라인이다).

기준선(`after-step2`) 대비 이동: 게이트 라벨 8줄 · 정밀도 출력의 신규 지표 열 · 실험 20 (d)의 ±k 표 · 회귀 러너 실행 건수 11 → 14 · 감사 대상 편수와 ADR 편수(14 → 15). **셀별 지표 값은 한 칸도 안 움직였다.**

### 🔄🔴 2026-09-09 — F38 번복: 라운드의 절반을 취소한 측정이 **인공물이었다**

바로 위 항목의 지연 게이트는 **FAIL이 아니라 PASS**다. 근거는 [`.omc/plans/verifier-f38-overturn.md`](.omc/plans/verifier-f38-overturn.md).

**무엇이 틀렸나.** 기본 호스트가 `http://localhost:11434`였다. `localhost`는 `[::1, 127.0.0.1]` 순으로 풀리고(해석 자체는 **0.15 ms**로 공짜다) ollama는 **IPv4에만 바인딩**한다. 그리고 🔴 **이 기기는 거절된 TCP connect를 표면화하는 데 ~2.02초를 쓴다** — `connect(('::1', 11434))` **2,006.68 ms** vs `connect(('127.0.0.1', 11434))` **0.26 ms**이고, **닫힌 IPv4 포트도 2,029 ms**로 같다. IPv6 우선 순서는 *실패할 주소를 고를 뿐*이고 **느린 거절이 비용의 전부다.** → **2,082 ms의 97.1%가 ollama에 닿지도 못한 실패 connect였다.**

| 값 | 마감 시점 | 🟢 정정 (`127.0.0.1`) |
|---|---|---|
| 웜 p50 (게이트) | 2,082 ms · 예산의 5.2배 · **FAIL** | **55 ms · 예산의 0.14배 · PASS** |
| 0-연산 대조 (`GET /api/tags`) | *재지 않았다* | 15.3 ms → **순 연산 34.2 ms** |
| 배치 20건 | 이득 1.0배 → *"비용은 연산"* | **이득 4.0배** → *"비용은 호출 오버헤드"* (**정반대**) |
| 배선 없는 조회당 | ≈45.8초 | **≈1.27초** (한 요청에 묶으면 ≈0.23초) |
| 인코딩 ÷ 검색(0.13 ms) | 16,000배 | **≈263배** — [ADR-003](docs/adr/ADR-003-storage-engine.md)의 **결론은 그대로 옳다** |

- **배치 이득 1.0배는 ollama의 성질이 아니라 래퍼의 루프였다.** `embedding.py`의 `embed()`가 텍스트마다 따로 HTTP 요청을 보내서 *"20건 배치"*가 2.02초 벌금을 **20번** 냈다.
- 🟢 **품질은 한 칸도 안 움직였다.** `rel_dist.py`·`precision.py`를 `127.0.0.1`로 다시 돌린 출력이 `after-r2a/` 스냅샷과 **바이트 동일**하다 — θ 사다리 · 0의 원자 90.7% · min 코사인 0.2142 · `misinjection@q` 전부 그대로다.
- 🟢 **배선 0건은 살아남았다.** `memory.py`는 여전히 `embedding`을 import하지 않고 `db_put_vec`/`db_get_vec` 호출부는 **0건**이다. 후속 9에서 **살아 있는 것은 이 절반뿐이다.**
- 🔴 **모델 교체 질문은 철회한다.** 후보 5종이 `127.0.0.1`에서 **전부 PASS**(45~56 ms)이고, 품질은 프로브 **10개**로 이 다섯을 구별하지 못한다 — bge-m3 대비 부호 검정 **p = 0.500 / 0.250 / 1.000 / 1.000**, 7/10의 95% Wilson CI **[40%, 89%]**, 그리고 품질 차이 **전체가 10개 중 2개 프로브** 위에 서 있다. **bge-m3 유지.**

**고친 것.** 기본 호스트를 `127.0.0.1`로(5파일 · `OLLAMA_HOST` 덮어쓰기는 유지) · `rel_dist.py --latency`가 **전사가 아니라 측정**하도록 · 그 출력에 **0-연산 대조행**과 **측정 호스트**를 추가 · `embed_model_sweep.py`에 같은 대조행과 **프로브별 순위 목록**을 추가하고 크기 주석 정정(`snowflake-arctic-embed2` 305M → **566.70M**).

🔴 **그리고 이 라운드가 배운 것 — 일치는 정확성의 증거가 아니다.** 세 측정(F38 **2,082** · Critic 독립 **2,073** · 앞선 n=5 **2,125 ms**)이 **0.4% 안에서 일치**했고, 그 일치가 ADR-015에서 *"그래서 지우지 않는다"*의 근거로 인용됐다. 셋 다 같은 래퍼·같은 기본 호스트를 지났다 — **공유된 결함을 통과한 독립 재측정은 결함을 독립적으로 확인해 준다.** 이것을 잡는 것은 재측정이 아니라 **0-연산 대조**다(다섯 줄 · ~10초): *모델을 안 돌리는 호출이 같은 값을 내면 그 숫자는 모델에 대한 것이 아니다.* 이제 `experiments/rel_dist.py --latency`가 그 행을 찍는다.

⚠️ **R2b(단계 3·4·5)는 여전히 미실행이다.** 정정이 바꾼 것은 **실행 여부가 아니라 미실행의 사유**다 — 게이트 FAIL이 아니라 그냥 돌리지 않았다. 재개 여부는 다음 라운드의 결정이다.

---

## 남은 작업 (우선순위 순)

| | 작업 | 상태 |
|---|---|---|
| 1 | 낡은 문서 갱신 표시 (01·02·03·05) | ✅ |
| 2 | 사실 대장 + 평가 문항 + 합성 코퍼스 | ✅ |
| 3 | 검색 계층 실험 4개 | ✅ |
| 4 | 문서 간 링크 검증 | ✅ (40개 파일 전수) |
| 4-b | **소크 테스트 — 구현 × 실제 코퍼스** | ✅ **설계 결함 3개 추가 발견** |
| 4-c | **게이트·임계 스윕** | ✅ **실험 7·11 정정 + θ 0.15→0.05** |
| 4-d | **응답 품질 실측 (5번째 축)** | ✅ **L3 35% > L2 23%. Step 0 답 나옴** |
| 5 | dense 검색 추가 (하이브리드 비교) | 로컬 임베딩 모델 필요 — 미착수 |
| 6 | **LLM 생성 품질 코퍼스** | 미착수 (합성 템플릿으로 대체 중) |
| 7 | **품질 실험** (응답 품질·L3) | ✅ **완료** — 실험 13 |
| 8 | 행동 프로브 18개 (PPR·페르소나) | **다른 벤더 심판 필요** |
| 9 | A0R(규칙만) · G1(현행 재현) arm | 무료 쿼터로 가능. 26콜씩 |

---

## 차단된 것 — 사용자 결정 필요

**Gemini API 키 구매 여부.** 예상 $30~50.

키가 있으면 즉시 가능한 것:
1. **Interactions API 실측** (10분) — 과금 입력 토큰이 실제로 주는가. [ADR-008](docs/adr/ADR-008-provider-model-tiering.md)의 보류를 풀거나 굳힌다
2. **유저 간 접두사 캐시 실측** (10분) — [ADR-007](docs/adr/ADR-007-context-packing-cache.md)의 핵심 가정
3. **L3 실패율 측정** (`A1 Oracle` arm) — 나머지 실험 전체의 순서를 정한다
4. 한국어 토크나이저 효율 비교 (30분)

키가 없어도 가능한 것: 사실 대장, 코퍼스, 로컬 임베딩(KURE) 기반 `evidence_recall@k`, 토큰 회계, 캐시 접두사 안정성 시뮬레이션.

---

## 2026-09-09 (라운드 2 · R2b) — 격자를 돌렸다. **규칙이 "고르지 않는다"고 답했다**

R2a(계측·확정)를 마감한 뒤 R2b(격자·조건부)는 지연 게이트 FAIL로 취소돼 있었다. **그 게이트가 인공물이었고**
(`localhost`의 실패 IPv6 connect — 2,082 ms의 97.1%), PASS로 번복된 뒤 오케스트레이터가 재개를 지시했다.
단계 3·4·5·6b를 실행자 3 · 검증자 2 레인으로 돌렸다.

| 단계 | 무엇 | 결과 |
|---|---|---|
| 3 (레인 D) | `memory.py`에 검색 모드 3종 · `THETA_BY_MODE`(모집단 서명 튜플) · `stale_theta` · 토크나이저 정본 승격 | 🟢 **기본값 경로는 비트 단위 불변**(차등 시험 · 15질의 × 22사건, 점수를 hex로 대조해 불일치 0) |
| 4 (레인 E) | 검색 격자 30셀 (실험 21) · 사전 등록된 파레토 규칙 · 강등 금지 + 종료 77 · 전역 9종 복원 | 🟢 **완주 · 종료 0 · 30/30 · `degraded` 0** · 벽시계 ~16초 |
| 5 (레인 E) | 기본값 승격 판정 — 승격 3조건 + 선행 조건 3종 | ⛔ **기본값 `lexical` 유지** — 전선 5셀이라 승격 규칙이 먼저 닫는다 |
| 6b (레인 F) | 기준선 스냅샷 `after-r2b/` · ADR-015 격자 절 · `docs/11` 실험 21 절 · 편수 20 → 21 · 계획 위생 | 🟢 감사 종료 0 (**실험 21 · ADR 15**) · `run_all` **15건 · 실패 0** |

### 이 라운드가 실제로 얻은 것

- **파레토 전선 5셀** (#8 `embed` 13/28·10 · #5 `lexical_fixed` 12/28·8 · #6 · #9 6/28·3 · #15 3/28·2).
  사전 등록된 규칙은 *"전선 위에 여럿이 남으면 승격하지 않는다"*이고 **그대로 집행됐다.**
- **`embed`의 검색 품질은 나쁘지 않다** — #8의 근거회상 13/28이 **격자 전체 최고**다.
  `embed`를 막고 있는 것은 품질이 아니라 **배선**이다(`import embedding` 0건 · 벡터 호출부 0건).
- **선행 결론 하나가 반증됐다** — 하이브리드 기각의 근거였던 *"어휘의 기여가 0"*은 270쌍 프로브의 성질이고,
  378쌍 검색 모집단에서는 **어휘가 임베딩이 잃은 문항을 넷 들고 있다.** (기각 자체는 다른 조건이 붙들고 있다.)

### 🔴 그리고 이 라운드가 **적지 않기로 한** 것

**승격 조건 3(콜드 p95 ≤ 400 ms)의 판정을 결과로 쓰지 않는다.** 같은 기기·같은 명령 **18 draw**에서
`p95`가 276~540 ms를 오가며 **PASS 10 / FAIL 8**로 갈린다 — n=18에서 p95는 **표본의 최댓값**이기 때문이다.
안정적인 것은 **p50 ≈ 220 ms**다. 그 사이 전선 5셀과 최종 판정은 **18/18 불변**이었다.

> **모양은 맞고 값은 틀린 숫자에 지속되는 이름을 주지 않는다** — 이 저장소가 F21로 이미 한 번 겪은 실패다.
> 격자는 그 실패를 막으려고 강등 금지·`측정 안 됨`·종료 77을 걸었는데, **그 방어는 셀에만 닿고
> 지연 프로파일은 셀이 아니었다.** 분포는 `.omc/plans/baseline/after-r2b/cond3-draws.txt`에 남겼다.

### 마감에서 고친 것

- **낡은 상태 칸 13개** (계획의 범위 선언 · 시스템 상태표 · §7 파일 지도). 단계 3 행 셋은 **레인 D가 돌 때** 낡았다.
- **가드레일 둘을 다시 규정** — **G5**의 검증 명령이 *올바른 수리*(동결된 `_ENDINGS`를 **참조**하는 것)를
  위반으로 잡고 있었다. 그것을 피하는 유일한 길은 정의를 두 번 쓰는 것이고 **그것이 G5가 막으려던 바로 그것**이다.
  **G13**의 grep은 네 철자 중 하나만 잡고, 코드의 복원 목록에 계획이 요구한 `TOP_K`·`TAU_IMPORTANCE`가 **없다**
  (실측: `TOP_K = 999`가 복원 단언을 조용히 통과한다 — 누출은 없지만 그 검사는 **항등**이다).
- **표류 인용 7건** — 단계 3이 `memory.py`에 246줄을 넣으며 인용을 일괄 갱신했는데 **오프셋이 8줄 작았다.**
  검증은 *줄이 존재하는가*를 봤고 **그 줄이 무엇인가**를 표본 밖에서 놓쳤다.
  🔴 **이 저장소의 여섯 번째 표류 인용 사건이고, 처음으로 검증을 통과한 뒤에 발견됐다.**

### 다음 하나

**문서 벡터 배선**(`db_put_vec`/`db_get_vec` 호출부 · 쓰기 경로 사전계산). 이제 이것은 속도 작업이 아니라
**지연 조건을 잴 수 있게 만드는 작업**이다 — 배선 뒤의 준-콜드 p95 64 ms는 예산 상한의 0.16배라
표본 최댓값이 어느 쪽으로 튀어도 통과한다. 지금 조건 3은 **동전 던지기** 위에 서 있다.

---

## 2026-09-09 — 후속 마감: 미해결 셋을 닫고, 첫 유의한 검색 결과를 얻었다

> 실행: 레인 G(배선·조건 3 표본·결정적 정렬 키) · 레인 H(인용 감사기) · 레인 I(유형 라벨 프로브) ·
> 레인 J(통합·문서·스냅샷). **제품 코드의 검색 기본값은 이번에도 한 줄도 안 바뀌었다**(G16).

### 닫힌 미해결 셋

| # | 항목 | 열리는 조건이 요구한 것 | 결과 |
|---|---|---|---|
| 후속 9-b | 문서 벡터 배선 | `import embedding ≥ 1건` · `db_put_vec`/`db_get_vec` 호출부 `≥ 1건` | 🟢 `1건 / 6건`. 콜드 p50 **216.3 → 48.1 ms** · 조회당 텍스트 **264 → 93** |
| 후속 10 | `retrieve()`의 결정적 2차 키 | — (승격 라운드로 미뤄져 있었다) | 🟢 `(-s, event_id)`. **값 이동 0** — `event_id` ≡ rowid |
| 후속 11 | `file:line` 인용 검사기 | — | 🟢 `experiments/citation_audit.py` |
| 조건 3의 재현성 | n을 키워 판정이 한쪽으로 고정되는 것 | n=18 → **108** | 🟢 p95 **70.0 ms** = 예산 상한의 0.18배 · PASS 고정 (두 draw: 70.0 / 71.0) |

### 새 실험 둘 (실험 21개 → **23개**)

- **실험 22 — 유형 라벨 프로브 24개.** 하이브리드 재진입 조건 1의 계측기다. **이 저장소의 첫 통계적으로 유의한 검색 결과**가 여기서 나왔다: `embed`가 의역형 p=0.0312 · 추론형 p=0.0156 · 전체 13승 0패 p=0.0002로 `lexical`보다 낫고, **직접형에서는 네 모드가 완전히 동률**이다(승 0 · 패 0 · 동 8).
- **실험 23 — 인용 감사.** 🔴 그 도구가 밝힌 사실: ***"줄이 존재하는가만 보는 검사는 이 저장소의 표류 7건을 전부 통과시킨다."*** 변이 대조로 실증했다.

### 🔴 그런데 판정은 안 바뀐다 — 그리고 그 이유를 검사했다

유의한 결과가 나왔는데도 승격하지 않는다. **격자는 18문항으로 판정하고 프로브 집합은 24개로 유형을 갈랐다** — 두 모집단은 다른 질문에 답한다. 승격을 막는 것은 여전히 **파레토 전선 5셀**이다.

여기서 자연스러운 반론이 하나 선다: *"격자가 모드를 못 가른 건 격자 문항이 대부분 직접형이라서 아닌가?"*
**검사했고 반증됐다.** 프로브 집합 자신의 라벨 검사 눈금(정답쌍 어휘 `rel` — 직접형 0.723 / 의역형 0.101 / 추론형 0.087, 사이에 빈 구간 (0.286, 0.545))을 채점 18문항에 대면:

```
경계 0.412로 직접형 배정   1/18   (경계를 빈 구간 전체에서 움직여도 0~2/18)
18문항 정답쌍 rel          평균 0.123 · max 0.444(Q24) · min 0.000
rel이 정확히 0인 문항      8/18    ← 현행 θ=0.05 아래다
```

**반론은 방향이 반대였다.** 18문항은 직접형이 많은 게 아니라 거의 전부 의역형·추론형 대역에 있다.
⚠️ **이 배정 라벨은 `eval/`의 것이 아니라 이 라운드가 붙인 것이다.** `eval/questions.yaml`에 유형 라벨은 없고,
`eval/`은 **읽기 전용으로만** 열었다(A8 — `git status --porcelain -- eval/` 빈 출력).

### 자가 저작 편향 — 조건 1을 충족시킨 것이 자기저작이다

프로브를 쓴 사람이 `memory.py`의 검색 경로와 ADR-015의 결과 표를 **먼저 읽었고**, 유형 경계도 한 사람의 판단이며,
**가장 어려운 두 칸(E005·E006)을 저자가 gold에서 뺐다.** 선언은 `probe_set.py` 머리말이 정본이고
**매 실행 출력에 찍힌다**(SKIP 실행에서도). 그래서 **하이브리드 재진입 여부는 이 라운드가 판정하지 않는다.**

### 마감에서 고친 것

- **표류 인용** — 살아 있는 문서(`docs/**` · `SUMMARY.md` · `prototype/**` · `experiments/**`)의 의심 인용을
  **31건 → 0건**으로 내렸다. 레인 G가 *"출력 줄이라 고치면 바이트 동일성이 깨진다"*며 오케스트레이터로 올린 세 건
  (`precision.py` 둘 · `rel_dist.py` 하나)도 여기서 닫았다 — **틀린 설명을 출력에 두는 쪽이 더 비싸다.**
  그중 하나는 줄 번호만이 아니라 **문장 자체가 거짓**이 돼 있었다(후속 10이 닫히면서).
- **`gate_sweep.py:85`의 근사 오류** — 줄 번호는 맞고 산문이 부정확했다(`:381`은 `{name:<24}`가 아니라 `{gname:<24}`).
  **표류가 아니므로 번호가 아니라 산문을 고쳤다.**
- **카운트 사이트 11곳** — 실험 `21 → 23`, 키 없이 도는 실험 `15 → 17`.
  🟢 **감사가 실제로 발화했다**: 실험 개수만 올리고 키 CLAIM을 안 고친 중간 상태에서
  `🔴 키 없이 도는 실험 — 기대 15 · 다른 값 발견 17`로 멈췄다. 그것이 그 CLAIM이 있는 이유다.

### 🔴 안 한 것, 그리고 왜

**인용 감사의 종료 코드는 여전히 1이다.** 남은 의심 **132건은 전부 살아 있는 계획서**(`ralplan-*.md` 116 · `open-questions*.md` 16)에 있다.
모양으로 세면 **표 행 49 / 산문 83**이고, 가장 큰 단일 절이 `§1 사전 조사에서 확인한 사실`(24) · rev 로그 절이 ~18이다 —
**상당 부분이 *그 시점의 사실*을 적어 둔 자리이고, 나머지가 무엇인지는 이 라운드가 하나씩 열어 보지 않았다.**
그런 자리를 기계적으로 재고정하면 **그 기록이 지워진다**(도구가 마감된 레인 보고서를 제외하는 것과 같은 논거).
🔴 **그리고 기계적 재고정이 안전하지 않다는 것을 실측했다** — 감사의 제안을 **적용하지 않고 출력만** 보니
`embedding.py`의 **같은 옛 인용 하나가 자리에 따라 세 개의 서로 다른 새 줄로 제안됐다.** 닻이 그 자리의 산문에서 나오기 때문이고,
그것은 *"이 인용이 무엇을 가리키려 했는가"*가 아니다 — **오프셋을 8줄 틀리게 잡아 일곱 건을 만든 것과 같은 종류의 수리**다.

> **종료 규칙도 제외 목록도 바꾸지 않았다.** 숫자를 초록으로 만들려고 게이트를 다시 쓰는 것이
> 이 저장소가 이미 여러 번 기록한 실패 방식이다. **미해결 항목으로 올린다** — *"인용 감사의 제외 모델"*.
> 대가는 정직하게 적는다: `run_all.py` 전량 실행이 **실패 1건**으로 끝난다.

### 다음 하나

**교환비**(Q2-2). 조건 1이 충족되고 조건 3이 고정되고 배선이 들어온 지금, 승격을 막는 것은 계측기가 아니라
**오주입 1건과 회상 1건을 어떻게 바꿀 것인가**라는 제품 결정 하나다. 격자를 다시 돌릴 필요는 없다 —
**이미 돌아간 격자를 다시 읽으면 된다.**

## 2026-09-09 — 실험 24: 이 저장소의 첫 응답 수준 실험

검색 지표를 두 라운드 동안 정교하게 고쳐 왔는데, **그 지표들이 움직여도 응답이
안 움직인다**는 것을 처음으로 쟀다. 파레토 전선 5셀 + 기준칸에서 `qwen3:8b`로
108건을 생성하고 대장 규칙으로 채점했다(생성은 모델이, **채점은 규칙이** —
자기 채점이면 결과가 아무것도 아니다).

- `recall` `3/28 → 13/28`(4배)에서 **정답 진술은 3~4**. 부호검정 10건 중 `p<0.05` **0건**.
- ⛔ 답은 *"교환비가 1:1"*이 아니라 **"이 설계로는 잴 수 없다"**. 이유 셋:
  ① 사실 블록이 셀과 무관하게 정답·오답을 넣어 `검색만` 열이 2문항 ②
  파레토 전선에서 고른 탓에 `corr(recall, mis) = +0.929` ③ n=18에 검정력 없음.
- 🔴 **오주입 사용률 6%** — 꺼낸 오주입을 모델이 거의 안 쓴다. 오주입 지표의
  세 번째 문제(인공물 49 → 상수 0 → 응답 미도달). **판정은 안 했다.**
- 실험 23 → **24**, 감사 종료 0. 기준선 `.omc/plans/baseline/after-exp24/`.
- ⚠️ 생성 1건이 타임아웃으로 끊겨 체크포인트에서 이어 붙였다 — 깨끗한 실행과
  같은 산출물이 아니다.

## 2026-09-10 — 실험 25(Q2-2 재설계)와 인용 감사 제외 모델 확정

두 레인을 병렬로 돌렸다. 하나는 실험 24가 **자기 자신에게 진단한 결함 셋**을 걷어냈고,
하나는 인용 감사의 **제외 모델**을 정했다.

### 실험 25 — 셋 중 둘이 열렸다

- ① **사실 블록을 셀 축에 올렸다**(`INJECT_KNOWN_FACTS` 두 수준, 이미 있던 전역) →
  `검색만` **최대 14문항**(≥5인 셀 9개). 🔴 **`memory.py`는 한 줄도 안 고쳤다** — G16 무관.
- ② **셀 선택 규칙을 바꾼 게 아니라 고르는 일 자체를 없앴다**(1단 18셀 전량) →
  `corr(recall, mis) = **+0.645**`. 전선은 비지배 집합이라 **정의상** 두 축을 같이 움직인다.
- ③ ⛔ **안 열렸다** — 최대 불일치 **9쌍**, 10에서 하나 모자람. 312건 중 유의 0건.
- 🟢 **두 축이 처음으로 갈라졌다**: `a(recall) = +0.1717 ± 0.0481`(표준오차의 3.6배),
  `b(mis) = −0.0002 ± 0.0099`. **분모가 0인 비율**이지 유한한 교환비가 아니다.
- 🔴 **대신 새 결함이 드러났다: 채점기가 의역·추론을 못 잰다** — 의역형 **0/108** ·
  추론형 **0/126**. 축자 매칭인데 그 두 유형은 정의상 대장의 낱말을 안 쓴다.
  **다음에 고칠 것은 셀도 n도 아니고 채점 규칙이다.**
- 🔄 **오주입 사용률 재측정: 107번 중 0번.** 6%도 그 자리에서 재현된다. ⚠️ 그러나
  **유의하게 다르다고는 못 한다**(불일치 0~3쌍). 검정이 필요 없는 부분만 남긴다 —
  **발화 26번이 전부 사실 블록이 있을 때 일어났고, 사실 블록만 없애면 0이다.**
- 🔴 **옮겨 적기 쉬운 오해 하나:** 6→18셀은 **비교 수를 10→312로 늘렸을 뿐 어느 비교의
  n도 안 늘렸다.** **비교가 많은 것은 검정력이 아니다.**
- 시험 31 → **58개**. 변이 12건 전부 발화 확인.

### 인용 감사 — 제외는 **파일이 아니라 인용 하나**에 붙는다

- 인용 **바로 옆**에 과거 표식이 있는 것만 뺀다(41개). 계획서는 **여전히 전량 감사**하고
  시험이 그것을 고정한다. 파일 단위 규칙은 `§단계` 절의 **살아 있는** 인용을 함께 먹는다.
- 제외된 41개를 **하나씩 전부 찍고**, 각 줄에 *"표식이 없었다면 어떤 판정이었나"*를 같이 찍는다
  (6개가 `의심`이었다). **보이지 않는 제외가 지난 표류 여섯 건이 살아남은 방식이었다.**
- 🔴 **여덟 번째 "발화할 수 없는 검사".** 기존 제외 정규식 `(?:이전|원래|과거|before)\s*$`가
  **경로형 인용에 한 번도 발화한 적이 없다** — 경로 매칭이 백틱 *뒤*부터 시작해 앞 창이
  항상 백틱으로 끝났다. **백틱 하나가 앵커를 죽였다.**
- 음성 대조가 하중을 받는다: 제외 구역 안의 진짜 표류는 원장에 이름이 찍히지만
  **게이트는 0으로 통과한다** — 알고 포기한 것이고, 시험이 그 사실을 못박는다.

### `run_all`의 실패 1 — 종료 코드가 아니라 어휘를 고쳤다

> 영구히 빨간 **올바른** 도구는 도구의 버그가 아니라 러너의 **보고 어휘** 버그다.

`실행 N건 · 실패 0건 · 감사 지적 1건 (알려진 상태 1건) · SKIP 0건`.
`citation_audit.py`의 종료 1은 그대로다. `AUDIT_FILES`·`AUDIT_KNOWN_RED`를 `GRID_FILES`와
같은 방식으로 import 시점에 대조하고, **양방향 음성 대조로 발화를 확인했다.**
⚠️ `consistency_audit.py`는 **일부러 감사로 표시하지 않았다** — 거기가 빨개지면 그것은
새로 깨진 것이므로 `실패`가 맞는 어휘다.

### 마감에서 고친 것

- `키 없이 도는 실험` CLAIM이 **또 발화했다** — 실험을 24→25로 올리고 이 값을 안 고친
  중간 상태에서 `기대 18 · 다른 값 발견 19`로 멈췄고, 내가 놓친 `README.md:182`를 짚어 줬다.
- 실험 24 → **25**. 감사 종료 0. 기준선 `.omc/plans/baseline/after-laneMN/`.

### 다음 하나

**채점기.** 교환비를 판정하려면 검정력이 필요하고(③), 검정력을 만들 문항은
의역·추론인데 **오늘의 채점기가 그 둘을 0으로 만든다.** 자기 채점 금지는 그대로이므로
이것은 *"채점을 누가 하는가"*를 다시 여는 질문이다.

---

## 2026-09-10 — 요약 계층 (ADR-016): 채택돼 있던 결정을 **돌게 만들고**, 서빙 델타 하나를 판정했다

네 단계를 병렬 레인으로 돌렸다(S0 저울 · S1 스키마 v5 · S2 생성 경로 · S3 서빙 정책).
🔴 **판정 단계는 S3 하나였고, 이 라운드가 판정한 것도 그 하나다.**

### 판정한 것 — 서빙 정책의 델타

- `STALE_SERVE_POLICY`를 두 값으로 돌려 stale 파생물의 **주입 0회 → 10회 · 제외 10회 → 0회**.
  분모는 **심은 digest 2행 × 대장 전이 5회 = 10**이고, 바꾼 것은 전역 **하나**다.
- 🔴 **귀속의 근거는 «재현»이다.** 옛 동작(`exclude_all`) 쪽이 `ADR-013`의 기록값을
  **같은 분모에서 그대로 재현**했다. `fsm_probe.py`는 그 값을 **ADR 파일에서 읽고**,
  못 읽거나 안 맞으면 **종료 1**을 낸다 — STOP 조건을 코드가 진다.
- ⚠️ 서빙 **동작**의 델타이지 **품질**의 수가 아니다. *"경고와 함께 나가는 편이 낫다"*는
  근거는 그 표에 **없다.**

### 판정하지 **않은** 것 — 요약 품질

- **M1~M4 지표는 정의만 됐고 하나도 안 돌았다.** 채점 실험(실험 26)을 **후속으로 뺐다** —
  계획이 스스로 *"그 단계는 판정하지 않는다. 관측한다"*고 적었고, **판정하지 않는 그
  단계가 가장 비싼 신설물**이었다. **이름을 정확히 붙였더니 그 이름이 범위를 잘랐다.**
- 그래서 ADR-016 §결과에 **«비운 자리»**가 이름을 갖고 서 있다. 24세션 전량 생성이 찍은
  `표제 3/3`은 **형식 위생이지 지표가 아니고**, `길이 97–189글자`는 길이다.
- **«P4가 실행 가능해졌다»·«세션 요약 소실이 사라졌다»에도 숫자를 안 붙였다** — 보존되는
  것과 쓸모 있는 것은 다른 주장이고, 후자를 잴 심판이 없다(X3).

### 발견 — 채택은 실행이 아니었다

ADR-013의 P4(*"모든 세션 요약에서 재작성"*)가 **현행 스키마에서 실행 불가능**했다.
`digest`의 PK가 `(chat_id, kind)`라 세션 요약이 **채팅당 1행**이고, *"모든 세션 요약"*의
크기가 **1**이었다 — 스키마가 지원하는 유일한 재작성이 **ADR-013이 기각한 증분**이었다.

### 게이트 하나가 기본 설정에서 **발화할 수 없었다** — 그리고 안 고쳤다

주입 블록 수 축에서 기준선 1.2배를 **처음 넘는 곳이 `b=4`**인데 기본값에서는 구조적으로
`b ≤ 3`이다. **폐기도 문턱 재유도도 안 했다** — 축 위에서는 발화하고(항등이 아니다),
**발화하도록 문턱을 고르는 것은 통과하도록 고르는 것과 같은 규칙의 양면**이다.
→ 주어를 `b`에서 `M`으로 바꿔 적었다. 산 값은 **한계 비용 222 ntok**, 나오는 계약은
`DIGEST_INJECT_MAX ≤ 1`, 🔓 살아나는 조건은 그 값이 **2 이상**이 되는 순간이다.

### 마감에서 고친 것 (S5)

- **ADR 15편 → 16편.** 🔄 옛 값 15는 여기 남긴다(색인 표와 근거 수준 합계를 같은 패스에서 맞췄다).
- 🔄 **소크 저장량 턴당 290B → 302B.** 빈 `digest_session` 한 테이블이 **SQLite 페이지 2개
  = 8,192B**다. 행은 0이고 **동작은 한 줄도 안 움직였다** — 옛 값은 감사기 주석에 남겼다.
- 🆕 **CLAIM 하나 추가 — «서빙 정책 델타 주입(옛→새)» = 10** (3곳 일치). 🔴 **화살표가
  규정의 일부다:** 맨 `제외 (\d+)회`로 잡으면 **새 기본값의 `제외 0회`와 섞여** 정상인
  문서를 빨갛게 만든다. 음성 대조 둘을 심어 둘 다 발화시켰다(값 변이 → `기대 10 · 발견 12`,
  넓힌 정규식 → ADR-013·ADR-014에서 `0` 충돌).
- **낡은 산문 둘을 고쳤다.** `docs/11` E절과 `ADR-014`가 «주입 0 / 제외 10»을 **«현재 서빙
  동작»**으로 적고 있었다 — 기본값이 뒤집혔으므로 지금은 낡았다. 🔄 **옛 값은 그대로 두고
  «무엇의 이름인가»만 고쳤다**(그 값이 대조 기준이다). ⚠️ 정합 감사는 이것을 못 잡는다 —
  CLAIM 대상이 아니었다.
- **기준선:** `.omc/plans/baseline/after-summary-layer/`.

### 다음 하나

**채점기**가 그대로 남는다(rev8부터). 그리고 새로 열린 것 하나 — **«재생성 잡을 실사용에서
누가 부르나»**. 프로토타입 범위 밖이라 이 라운드가 안 닫았다.

### 독립 검증 (READ-ONLY 레인) — `PASS-WITH-FINDINGS`, 지적 6건 마감

검증자가 **레인 보고를 믿지 않고 직접 심어서** 확인했다.

- **판정 앵커가 코드에 물려 있다** — `ADR-013:139`를 `제외 9회`로 바꾸자 `fsm_probe`가 **종료 1**. 복원 후 SHA-256 일치.
- **G19′ ①②③이 서로 분리되어 발화한다** — 변이 ②는 ①을 초록으로 남긴다. 그리고 러너 실패 **0→1**.
- **오발화 없음** — DB 일곱 조합(빈/legacy/24세션/30세션 초과 × 전이)이 전부 통과하고, **legacy DB가 상한 N+2에 정확히 닿고 안 넘는다.** rev3이 rev2의 «정확히 2»를 상한식으로 고친 것이 옳았다.
- **ollama 없이도 G19′가 집행된다**(깨끗하면 77, 변이면 1) — 집행이 체크포인트 **앞**에 있다.
- **문서가 «안 잰 것»을 안 잰 것으로 적었다** — M1~M4 수치 **0건**, «비운 자리»가 실제로 비었고 `표제 3/3`·`길이`를 **선제적으로 M1이 아니라고 부인**한다.

🔴 **그리고 이 라운드가 아홉 번째 «발화할 수 없는 검사»를 스스로 만들었다.**
`290 → 302` 정정 때 넣은 괄호가 CLAIM 정규식의 구분자 `[,·]`를 밀어내 **커버리지가 3곳 → 2곳으로 조용히 줄었다.** 값이 여전히 302라 감사는 `✅ (2곳, 일치)`를 찍었다 — **곳수가 줄어든 것을 아무도 못 본다.** 「굵게 표시가 편수 정규식을 가른」 사고와 **같은 계보**다.

> **줄이 아니라 기제를 고쳤다.** `CLAIMS` 튜플에 **선택적 «최소 곳수»**를 더해 **값이 맞아도 커버리지가 줄면 운다.**
> 음성 대조: 구분자를 다시 빼니 `🔴 302 (2곳 — 기대 3곳 이상)` · 종료 1.

⚠️ **정정을 적는 행위가 정정을 깨뜨린다 — 이 라운드에서 세 번.** 설명에 쓴 백틱 심볼이 «더 좁은 닻»으로 뽑혀 **고쳐 놓은 올바른 인용을 빨갛게** 만들었고, **한 줄에 같은 파일 인용이 둘이면 감사기가 더 좁은 닻 하나로 둘 다 판정한다.** 평문화 + 줄 가르기로 닫았다.

**마감 시점:** `run_all` 실행 19건 · **실패 0건** · 감사 지적 1건(알려진 1건) · 정합 감사 exit 0 · 인용 감사 **124건 동일 집합**(살아 있는 문서 0 · 계획서 0) · 시험 prototype 151 + experiments 159 OK · `eval/` 무변경 · 커밋 없음.

---

## 일반화 취약성 라운드 — 마감 (2026-09-10)

**물음:** *"어떤 기억을 저장하거나 검색할지 판단하는 휴리스틱이 정말 장기적으로도 적절한가? 대화의 종류가 다양해져도 일반적으로 동작하는가?"*

레인 넷이 병렬로 돌았고 **둘은 문서를 일부러 안 건드렸다** — 이 마감이 그 둘을 접었다.

| 레인 | 산출 | 문서 |
|---|---|---|
| 실험 28 `A4` | ②(재압축)가 **24세션 구간에서도 참** (`A0′` 0/10 → `A4` 5/10) · ④ 창은 결과를 안 바꿈(본문 **바이트 동일**) | 레인이 직접 씀 |
| 실험 29 게이트 포화 | 평평함은 «오늘 무엇이 저장되는가»의 성질 · 트리거 조건 어휘 **76**(구간 55–115 · 낙관적 경계) | 레인이 직접 씀 |
| 🔴 실험 30 박힌 상수 감사 | 상수 **31개** 분해 · **미유도 24** · sha256 고정물의 구멍 | **이 마감이 씀** |
| 🔴 실험 31 두 번째 코퍼스 | `eval2/` 등록 · `UBIQUITOUS` 효과 **0** · θ는 재현 | **이 마감이 씀** |

### 🔴 하나를 고쳤다 — sha256 고정물이 «함수 원문만» 얼리고 있었다

`scoring.UBIQUITOUS = set()`으로 갈면 **해시 시험이 초록인 채** `survived_v2`가 옛 위양성 3건을 되살린다. `inspect.getsource(함수)`가 못 박는 것은 원문이고 **모듈 전역은 그 밖**이다.

→ `TestFrozenClosureIsFrozen` 신설. **손으로 적은 목록이 아니라 `co_names`를 따라 도달 가능성으로 전개**해 상수·도우미 함수·**다른 파일의 형태소 표**까지 한 해시에 넣는다(닿는 이름 **5개**·**9개**). 집합은 **정렬해서** 찍는다(`repr(set)`은 `PYTHONHASHSEED`의 함수라 안 그러면 «오늘 운이 나빴다»로 빨개진다).

- **원문 해시는 지우지 않고 함께 둔다** — 둘이 다른 자리를 본다.
- 심은 변이 **일곱 전부 발화 · 조용한 쪽 둘**. 특히 `_ORDINALS`(v3만 읽는다)를 비우면 닫힌 해시가 **초록으로 남는다** — 전부를 해시에 넣으면 «무엇을 고쳐도 빨간 검사»가 되고 그런 검사는 곧 꺼진다.
- `test_scoring` **13 → 18건.** 세 파일 sha256 복원 확인.

### 🔴 «이 프로토타입의 경계»를 신설했다 (`docs/12`)

무너지는 것 / 버티는 것을 가르고, **구조적으로 못 닫는 것 넷**에 이름을 붙였다 — **U2**(누가 채점하는가) · **U8**(실사용 스케줄러) · **n=5 한계** · **실사용 로그 없음**.

> **«미해결»과 다른 목록에 둔다.** ADR-016의 미해결은 **열리는 조건이 저장소의 출력이 직접 세는 값**이다. 위 넷은 **방아쇠가 저장소 밖에 있다.** 같은 목록에 두면 «언젠가 되겠지»가 되고, 그러면 아무도 **밖에서 무엇을 가져와야 하는지** 안 적는다.

### 마감에서 고친 것

- 🔄 **카운트 동기화 — 실험 29 → 31 · «키 없이» 23 → 25.** 늘어난 둘은 **키도 의존성도 안 쓴다**(`needs_extra=False`). 옛 값은 `docs/11` 머리말의 정정 줄에 남겼다. 🟢 **CLAIM이 실제로 발화했다** — 문서만 25로 올린 중간 상태에서 감사가 `기대 23 · 다른 값 발견 25`로 멈췄다.
- 🔄 **이 문서 제목의 «23건의 실험»이 낡아 있었다.** 실험이 23개이던 때의 수이고 여섯 라운드 동안 아무도 안 고쳤다. 🔴 `consistency_audit`의 `실험 개수` COUNT는 **`실험 N개` 꼴만** 보므로 **«N건의 실험»은 그 감사의 눈 밖**이다 — 적어 두는 것이 지금 할 수 있는 방어의 전부다.
- 🔄 **`ralplan-summary-layer`의 판번호가 두 곳에 있고 한 곳만 고쳐져 있었다** — 이력표에 `rev11` 행이 들어왔는데 제목·상태는 `rev10`이었다. **rev12**로 맞추고 그 사실을 적었다.
- **`run_all.STEPS` 25건** — `hardcoding_audit.py`·`corpus2_probe.py` 등록(다섯 원소 · `needs_extra=False` · 격자 아님 · `audit=False`). **대조 단언 둘에 위반을 심어 둘 다 발화 확인**(`GRID_FILES`·`AUDIT_FILES`).
- **인용 위생** — 이 라운드가 민 줄 **다섯을 스스로 고쳤다**: `ralplan-summary-layer`의 `run_all.py` 인용 4개(`:277-279` → `:326-328`)와 `corpus2_probe.py`의 닻 1개(`retrieve` → `TAU_IMPORTANCE`). ⚠️ **고치는 편집이 또 줄을 밀었다 — 이번에도.** `run_all.py`의 이동 대장 주석을 늘린 것이 대상을 `:324`에서 `:326`으로 옮겼고, `consistency_audit.py`의 CLAIM 주석을 늘린 것이 `:170`을 `:180`으로 옮겼다. 후자는 **대체 블록의 줄 수를 원본과 같게(6줄) 맞춰** 한 번에 닫았다.
- **기준선:** `.omc/plans/baseline/after-generalization/`.

### 다음 하나

**«채점기»가 rev8부터 그대로 남는데, 이 라운드가 그것을 «미해결»에서 «구조적으로 못 닫는 것(U2)»으로 옮겼다.** 여기서 더 시도할 자리가 아니라 **밖에서 심판을 들여와야 하는 자리**라는 뜻이다.

**마감 시점:** `run_all` 실행 **25건 · 실패 0건** · 감사 지적 1건(알려진 1건) · SKIP 0건 · 정합 감사 exit **0** · 인용 감사 의심 **148 → 143**(살아 있는 문서 **0** · 새로 생긴 것 **0**) · 시험 experiments **397** + prototype **151** OK · ADR-016 표제 **6** · `eval/` 무변경 · 커밋 없음.

---

## 2026-09-10 — 설계↔구현 괴리 라운드: 네 파도 + 처분표 (마지막 파도)

다른 세션이 설계↔구현 괴리를 두 문서로 분석했다(`.omc/research/design-vs-impl-gap-2026-09-10.md` · `non-search-gap-2026-09-10.md`). 그 뒤 다섯 레인이 차례로 돌았다. 레인 노트: `.omc/notepads/{leak,wave2,wave3,wave4,wave5}/progress.md`. **커밋 없음.**

### 네 파도가 고친 것

- **leak — 삭제 → 색인 누수.** `delete_item`이 색인 복사본(`event`)을 `stale`에만 넣고 읽기 경로는 `stale`을 안 봐서 **지운 사실이 계속 검색되고 게이트를 발화시켰다**(«바꾸면 빠지고, 지우면 남는다»). `delete_item`이 `_invalidate_derived`를 재사용한다. 게이트 어휘는 정의가 두 벌이었고 **두 축이 엇갈려** 있었다(프로덕션: 방 무필터 · 삭제 필터 / 스윕: 방 필터 · 삭제 무필터) — 연구 문서는 절반만 적었다. 기록값 이동 0칸(사전 등록대로). 시험 `prototype/test_deletion.py`.
- **wave2 — 전이 → 요약 재생성을 쟀다.** 전이당 LLM 호출 **25회**(세션 24 + lifetime 1). 사전 등록(«본문 바이트 동일이면 근거 없음»)이 «근거 있음»을 냈다 — 뒤에 **발화할 수 없는 조건**으로 밝혀졌다. 옆에서 찾아 고친 것: 전이의 `INSERT OR REPLACE`가 `fact:N 삭제됨`을 덮어 **지운 사실을 경고와 함께 다시 서빙**했다(ADR-016 U12). 동기 층 모의 팔 `experiments/meta_arm.py` — `used_memories`·`new_debt`가 처음 돌았다. `soak.py`가 5절에서 `TypeError`로 죽고 있었다(`run_all` 밖이라 아무도 몰랐다). `PYTHONHASHSEED`에 따라 움직이던 표 하나를 결정적으로.
- **wave3 — 🔴 F39: «temperature 0 + seed = 재현»은 거짓이다.** 결정성은 «같은 요청 + **같은 서버 캐시 상태**»에서만 성립한다. 콜드(적중 0)와 웜(적중 493)의 본문이 갈리고, 적중 길이가 같아도 실행이 다르면 또 갈린다. `prompt_eval_count`로는 안 보인다. 기록값이 안전한 이유는 **체크포인트 재채점**이고, 기록된 생성의 캐시 조건은 어디에도 없어 docs/12 §3 «못 닫는 것»에 올렸다. 재현 `experiments/cache_state_repro.py`. 그 위에서 전이 전파를 **관측 가능한 기준**(프롬프트 바이트)으로 재판정 → 세션 24/24 · lifetime 1/1 프롬프트 동일 · 본문 표류 23/24 → **전파를 뺐다**(25 → 0회) — lifetime의 세션 경계 방아쇠(U13)를 **먼저** 만들고. 삭제 → 전이 → 지운 사실은 여전히 안 나온다(`TransitionAfterDeletionTest`, 스위치 두 값).
- **wave4 — 부채 트리거 · 호칭 가드 · 씬 40 · 콜드 스타트 기록.** 백오프를 세션 경계로 · `Nd` · `debt:N → paid` · 평가 못 하는 트리거는 강등 기록. 실험 10의 «시계가 모호해 안 쏜다»는 실은 **«늦게 쏜다»**였다(S17 · 기대 S15 · 시뮬이 기대 회수에서 잘랐다). `called_as`·`last_emotion`에 씬 가드. 콜드 스타트 구현은 **실험 17의 어느 칸과도 같지 않다**(ADR-012 끝 절) — 코드는 안 고쳤다.

### 마지막 파도 — 처분표 (`docs/17-gap-disposition.md`)

🔴 **이 파도의 문제는 «무엇을 고쳤나»가 아니라 «무엇이 어디에 적혀 있나»였다.** 두 연구 문서의 항목 마흔여 개가 `.omc/research/`에만 있었고, 일부는 닫혔고 일부는 틀린 주장이었는데 원장에는 없었다 — 괴리가 처음 생긴 방식 그대로.

- 필수 범위 행 예순여덟: ✅ 19 · ⛔ 3 · 🔓 33 · ⏸ 13 · ❔ 0(딸린 부분 주장 다섯은 ❔로 따로). 본문 잔여 열아홉(🔓 16 · ❔ 3). 문서 정정 스물셋(했다 16 · 이미 4 · 틀려서 안 함 2 · 섞임 1).
- **연구 문서의 틀린 주장 열다섯** — 넷은 이 파도가 코드로 새로 찾았다: «같은 데이터, 같은 질문, 다른 채점기»(문항 10 vs 18 · 색인 단위도 달랐다) · «df=1이라 BM25든 coverage든 순위가 같다»(질의 쪽 겹침 항은 20/27이 df ≥ 2) · «평가 DB 밖에서는 매 조회 stale θ»(기본 모드는 서명이 없다) · «`superseded_by` 읽기 0»(데모·소크가 읽는다 — 런타임 0은 참).
- 🔓는 ADR 열 편의 «미해결»(003·004·005·006·007·009·010·014·015 · 016 U14) · ADR-013 끝 절 · docs/12 §3 사례 셋 · `open-questions.md` §I에 올렸다. ADR-016 표제 여섯 유지.
- **표류 인용 21건**(wave2가 남긴 `LEAVE`) — 전부 `experiments/*.py`(살아 있는 문서). 지금을 말하는 13건은 고쳤고(감사로 `확인` 17건 — 연구 문서 §5의 `probe_types.py` 머리 둘과 독립 검증이 찾은 `LEAVE` 밖 표류 둘 포함), 그때를 말하는 8건은 옮기지 않았다.
- **엔진 비교(`engine_arms.py`, 941줄)는 돌리지 않고 미뤘다.** 다시 뽑은 근거: 색인 22행 · bigram 188종 · df=1 162종(86.2% — 살아 있는 21행이면 182 · 156 · 85.7%). 그러나 판가름하는 수는 **IDF가 순위를 움직일 수 있는 문항 3/18**이다. 다시 여는 조건 «그 문항 ≥ 6»(부호검정 최소 쌍) · 색인 행 수 N은 **유도하지 못한다.** 🔄 독립 검증이 짚었다 — 3/18은 IDF의 **상대 가중** 몫에 한정된 수이고(θ 컷·`rel`/`imp` 비중을 통해 상수배로도 결과가 움직인다) 팔 사이 불일치의 상한이 아니다. 처음 적은 «필요조건»을 정정으로 남겼다. 🔴 그 레인의 노트는 «팔 실행 · 문서 갱신 ✅»라 적었지만 체크포인트도 docs/11 절도 저장소에 없다 — 노트판 «컬럼 = 구현».
- 새 CLAIM 하나(`엔진 라운드 — IDF가 움직일 수 있는 문항` · 기대 3 · 최소 7곳) — 값 치환 · 곳수 감소 둘 다 발화 · 분모 없는 조건 문구는 조용(음성 대조 셋).
- 코드: `memory.py` 독스트링 한 줄(낡은 F37 문장 · 줄 수 불변)과 인용·출력 글자뿐. 동결 해시 여덟 착수 전 = 마감. 새 실험 번호 없음. 기준선 `.omc/plans/baseline/after-gap-round/`.

---

## 2026-09-10 — 축 라운드: 레인 A·B + 마감 파도 (close2)

병렬 레인 둘이 끝났고 둘 다 «내 소관 밖»으로 처분표 · 카운트 · CLAIM · ADR-003 · `run_all` 등록 · 기준선을 남겼다. 🔴 레인 A는 «회귀를 배경으로 돌리고 알림을 기다린다»며 턴을 끝내 **최종 보고를 못 썼다** — 노트(`.omc/notepads/laneA/progress.md`)가 유일한 기록이었다. 마감 파도 노트: `.omc/notepads/close2/progress.md`.

### 레인이 만든 것
- **레인 A:** `memory.py` 끝 절 스위치 셋(`THETA_ON_SCORE=False` · `W_REC=0.0` · `RECENCY_HALF_LIFE=240` — 전부 기본 끔, 편집 전후 `retrieve()` 반환 바이트 동일) · `experiments/theta_position.py` · `prototype/test_retrieve_switches.py`(9건) · ADR-005 · ADR-015 🔄.
- **레인 B:** `experiments/{eval_controls_audit,predicate_vocab_cost,retrieve_scaling,vecdim_ppr_probe}.py` + 시험 35건 · ADR-004 · ADR-009 🔄.

### 마감 파도가 한 것 (코드 결함 수리 0)
- 재현기 다섯을 다시 돌렸다(G11) — 레인 노트와 값이 같다. 다른 곳: 브리프의 «네 실행 범위 2,241~3,094»는 두 분포(eval 분포 · 전부 τ 통과)를 섞은 범위였다 · 레인 A가 적은 사본은 `rank_all` 하나였는데 `make_retrieve`도 사본이다 · 지연 스크립트는 같은 코드의 두 실행에서 교차의 «첫 N»이 3000 ↔ 5000으로 뒤집혔다.
- `docs/17`: ❔ 아홉 → 넷(❔1–❔5 닫힘 · 다섯 다 원본 주장이 참) · 🔓 다섯에 값(🔓 유지) · ⏸ 셋에 🔄(검§1-6b · 검B2 · 검B3 — B3은 보류 이유와 «다시 볼 때»를 바꿨다) · 새 행 둘(축-1 엔진 하니스 점수식 사본 · 축-2 `_cosine` 차원 검사 없음) · §12.
- `ADR-003` «미해결»: 교차점 표(두 실행 · p50·p95 · n) · 재검토 트리거와의 관계(문자 그대로는 발화 안 함 — 주어가 pgvector) · 상태 «제안됨» 유지와 근거 · 차원 검사 항목. `open-questions.md` D6 행 끝 🔄.
- 새 실험 번호 없음 — 근거와 반대 논거를 `docs/11` 끝 «축 라운드» 부록 머리에. `실험 개수` 31 · `키 없이 도는 실험` 25 불변.
- `run_all`: 넷 등록(`needs_extra=False`) · `retrieve_scaling.py` 미등록.
- 새 CLAIM 하나(`θ 위치 — 가중치 축 True 바뀐 문항` · 기대 18 · 최소 9곳) — 음성 대조: 값 치환 발화 · 곳수 감소(False 쪽 치환) 발화 · 타임라인 문서와 맥락 없는 `True 19/36`은 조용 · 러너 부분 실행에서 등록 단계의 종료 1이 «실패 1건»으로 세어지고 심지 않으면 0건(5/5 · 표식 계수 0 → 1 · 복원 sha 일치).
- 🔴 **내 SUMMARY 문구가 진짜 표류 하나를 «해석 불가»로 숨겼다** — `ADR` 낱말이 SUMMARY에서 12 → 15줄이 되어 인용 감사의 식별자 상한(12)을 넘었고, 계획서(`ralplan-memory-modules.md`)가 SUMMARY의 옛 줄을 가리키던 의심 하나가 사라졌다. 낱말을 바꿔 12로 되돌려 의심으로 돌아온 것을 확인했다. 레인 A가 `score`로 겪은 것과 같은 기제다.
- 마감: `run_all` 실행 **32건 · 실패 0건** · 감사 지적 1건(알려진 1건) · SKIP 0건 · 정합 감사 exit 0 · 인용 감사 의심 195 → 197(살아 있는 문서 **0** · 늘어난 둘은 `run_all.py`가 18줄 밀리면서 **우연히 «확인»이던** 계획서의 옛 인용 둘 — 되닻 안 함) · 시험 proto 204 · exp 441 OK · 동결 해시 여덟 착수 전 = 마감 · `prototype/*.py` sha 불변 · `eval/` 무변경. 기준선 `.omc/plans/baseline/after-axes-round/`.

## 2026-09-11 — 실험 32: 엔진 요인 분해 (w7engines · 한 레인이 트리 전체)

- **무엇:** `experiments/engine_arms.py`를 다시 썼다(옛 판은 eval 18문항 · 사슬이 틀렸다 · `main` 없음). 팔 일곱 A0 · A1 · B1 · B2 · B3 · C1 · C2(분석기 → 식 → 구현 한 칸씩) × 두 보기(V1 순위만 · V2 파이프라인) × 코퍼스 다섯 열(eval · eval2 · eval3 레짐 셋) × 홀드아웃 문항별 부호검정. 사전 등록은 레인 노트(`.omc/notepads/w7engines/progress.md`)에 먼저 적고 코드 상수로 박아 첫 화면에 찍었다.
- **판정:** 구현(엔진) 요인은 순위를 한 자리도 안 바꿨다(175문항) · 순위만 본 주 판정에서 방향이 난 칸 0 · 파이프라인에서 방향 13(검정 120 · 힘 있는 41) — 종합 대조는 근거 적중 방향 0 · 오주입 증가 둘.
- **값을 보고 안 것:** OpenSearch 2.19.6이 BM25에 `(k1+1)` = 2.2를 boost로 곱한다 → P4의 «값» 대조가 사전 등록대로 거짓으로 나왔고 2.2로 나눈 사후 진단을 따로 찍었다. V2의 B1은 등컷 규칙이 요청 f를 못 맞춰(실제 컷 0.9945 대 0.9759) θ 눈금이 섞였다 — 사후 서술로만 적었다.
- **심은 위반:** 12 + 조용한 쪽 1 — 전부 기대대로(표식 계수 0 → 1 · 신호가 조용한 쪽에서 0회). 🔴 첫 판의 M3 신호가 조용한 쪽에도 걸렸다 — 조용한 쪽 대조를 하니스에 넣은 뒤에야 보였다.
- **기록:** `실험 개수` 31 → 32 · `키 없이 도는 실험` 25 → 26(음성 대조 먼저) · `run_all` 등록(`needs_extra=True`) · F40 등재(계획서 rev15 · docs/12 F39 행 끝). 기본 경로 · 동결 해시 · 체크포인트 불변. 컨테이너는 띄운 채로 두었다.

## 2026-09-11 — 0911 라운드 마감 (w9close · 레인 다섯 뒤 카운트 동기화 한 번에)

오늘 레인 다섯(w6infra · w6code · w7engines · w8code · w8theta)이 돌았고 카운트 동기화 파일은 전부 이 마감 레인 몫으로 남겨졌다. 노트: `.omc/notepads/w9close/progress.md` · 인수인계: `.omc/handoff/NEXT.md`(새로 씀 · 옛 판은 기준선 폴더에).

### 다시 뽑은 것 (G11 — 레인 노트의 수를 옮기지 않았다)
- `theta_grid.py` — θ 레인 마감 실행과 표 값 바이트 동일(사본 `memory.py` 판이 달라도). 노트에 없던 값 하나: recency 축 · 끔 1/16 · 0/7 · 6/25 · 7/25 · 5/25.
- `predicate_default.py` — 코드 레인 실행과 소요 줄 말고 바이트 동일.
- `retrieve_memo.py` — C1 동일성 0/40 · 웜 p95 ×0.34~0.39 · 🔴 콜드 s=1 칸이 «늘었다»(×1.11) → «구별 안 됨»(×1.02)으로 뒤집혔다(띠 가장자리 — 세 실행 공통은 «오늘과 같거나 조금 느리다»까지).

### 처분 (docs/17 §15)
- ✅로 간 셋 — 축-1(엔진 하니스 점수식 사본) · 축-2(`_cosine` 차원) · 검B3(내용 주소 메모 · 웜 경로 · 딸린 🔓 셋). 셋 다 코드가 바뀌었고 그 변화를 되돌리면 우는 시험이 있다 — 마감 레인이 사본에 옛 모양을 심어 두 시험 모듈이 우는 것을 다시 봤다.
- 값만 — 검B2(⏸ · 실험 33) · 밖E10 · 검기타-5(스위치 `legacy`). 🔄 정정 — 검§1-8 · 검§1-9(«죽은 두 축»은 21행 코퍼스의 성질) · 엔진-2(✅ → ⛔ — 잰 것이다).
- 필수 범위 세면: ✅ 19 → 20 · ⏸ 13 → 12(🔓 33 · ⛔ 3 · ❔ 0 그대로).

### 번호 · 러너
- θ 격자 = **실험 33**(축 라운드 부록이 스스로 적은 «새 실험이 되는 조건»이 참 · 사전 등록 · 판정 규칙 · 홀드아웃) · `run_all` 등록(같은 줄 · 줄 수 불변). 검색 메모 · 표 밖 술어 · 1파도 수리 · 인프라 · F40은 번호 없는 부록(docs/11 끝).
- `실험 개수` 32 → 33 · `키 없이 도는 실험` 26 → 27 — 음성 대조 둘(중간 상태에서 감사가 멈춤). 새 CLAIM `θ 격자 — 현행 조합 «켬 나음» 열`(기대 0 · 최소 9곳) — 값 치환 · 곳수 감소 발화 · 조용한 쪽 둘. 러너 등록은 부분 실행으로 «실패 1건» 발화 확인.
- `retrieve_memo.py`(지연 — 칸이 실행마다 갈린다) · `predicate_default.py`(≈ 6~7분) 미등록.

### 마감 (전부 이 세션의 새 명령 출력)
- `run_all`: **34건 · 실패 0 · 감사 지적 1(알려진 1) · SKIP 2**(`digest_budget` · `response_quality` — F40 가드의 77 · 통과가 아니다) · 약 10분(17:56 → 18:06). `RESULTS.txt` 정규화 전후 대조에서 **값 이동 0** — 움직인 것은 러너 머리(33 → 34건) · 지연 열 · 두 감사 절(키 없이 27 · 새 CLAIM · 실험 개수 33 · 인용 1,364 · 해석 불가 858) · 컨테이너 가동 시간 · 새 실험 33 절(마감 레인 단독 실행과 경로·시간 줄 말고 같다). 실험 32의 A0 지연은 메모 뒤 p50 4.25~4.49 ms(기록 ≈ 18 ms는 메모 전 — docs/11 §I 🔄).
- 시험: prototype **230 OK** · experiments **542 OK (skipped=3)** — 셋은 `test_theta_grid`의 공유 프로세스 건너뜀이고 `IsolatedRun.test_module_passes_in_fresh_process`가 `ok`(verbose로 이름 확인).
- 정합 감사 exit 0 · 인용 감사 의심 **197**(목록 착수와 동일 · 전부 `.omc/` · 살아 있는 문서 **0**) · 확인 240 · 해석 불가 858.
- 동결 해시 여덟 착수 = 마감 · 체크포인트 JSON 27개 sha 차이 0 · 기본 경로 지문 `09c0f100…` 착수 = 마감 · `eval*/` 25파일 sha 차이 0 · `git status -- eval*/` 착수와 같음 · `memory.py` `461189c8…` · `llm.py` `dfd15492…` 불변.
- 🔴 러너 음성 대조의 자식 프로세스가 `-B` 없이 돌아 `eval3/__pycache__/`에 캐시 하나를 만들었다 — 지워서 25파일로 되돌렸다(노트).
- 기준선 `.omc/plans/baseline/after-0911-round/`.

## 2026-09-11 — 사용자 결정 다섯을 원장에 적음 (w10record · 결정 기록 레인)

정본 `docs/decisions/decisions-0911.md` · 노트 `.omc/notepads/w10record/progress.md`. 이 레인은 판단하지 않았다 — 옮겨 적었다. θ·엔진은 «이번 실험의 조건에서는 안 함»이고 설계 기각이 아니다(원장 어디에도 그렇게 적지 않았다).

- **1 ollama:** 다운그레이드 안 함 · 기록 판 키 재채점(`llm.key_runtime`) + 측정 허용(`llm.measure_prompt`) · 가드 유지 → docs/12 F39 행 끝 · 계획서 rev15 F40 끝 · README 한계 F40 줄 끝.
- **2 θ:** 실험 33의 조건에서는 쓰지 않는다 · 판단 범위 여섯 · 다시 열리는 경우 다섯 → ADR-005 «미해결» 끝 · ADR-015 핵심 7 끝 · docs/17 검B2(⏸ 그대로 · 이유 칸).
- **3 표 밖 술어:** `legacy` 유지 → ADR-004 «미해결» 끝 · docs/17 밖E10(⏸) · 검기타-5(🔓).
- **4 엔진:** 실험 32의 조건에서는 도입하지 않는다 · ADR-003 «제안됨» 그대로 · 첫 후보 Postgres → OpenSearch → ADR-003 «미해결» 끝 · ADR-006 끝 절 · docs/17 §16 새 행 엔진-9(⏸).
- **5 컨테이너:** 떠 있게 둔다(`memarch-os` · `memarch-pg`).
- **러너:** `run_all.py` 요약의 SKIP 설명을 자식이 찍은 이유 줄로 — 줄 수 중립 세 줄(루프 · 요약 둘) + `__main__` 가지 앞 끝 절(`_skip_reason`) · 이동 대장의 `subprocess.run` 줄은 제자리. 시험 `experiments/test_run_all_skip_reason.py` 6건 · 심은 위반 넷(이유 삼킴 · 옛 고정 문구 · 표식 우선 없음 · 루프가 이유를 버림) 전부 발화 · 조용한 쪽 실패 0.
- **기준선:** `run_all` 34건 · 실패 0 · 감사 지적 1(알려진 1) · **SKIP 0**(F40 SKIP 둘이 재채점으로 풀림 — `response_quality` 생성 0건 · 캐시 히트 1368건) · 스냅샷 `.omc/plans/baseline/after-decisions/`. 동결 해시 여덟 · 체크포인트 JSON 27개 · 기본 경로 지문 `09c0f100…` · `eval*/` 불변.

## w12close — w11 세 레인(w11a · w11b · w11c) 마감 (2026-09-11 밤)

- 레인 노트를 옮기지 않고 재현기를 다시 돌려 적었다(`memo_multiroom.py` sim·real · w11b 하니스 · `rel_dist.cos` 계측 · `probe_types.py` · ❔6 스캔) → docs/17 §17(검B3 딸린 ②에 값 · C5 s=1 «절벽 아님» 정정 · O1 · 축-2 딸린 · 사본 둘 · 출력 문구 셋 ✅ · ❔ 넷 닫힘 · 새 🔓 R13-a · R13-b · ⛔16 · ⛔17 → 틀린 주장 17) · docs/11 끝 부록 §H · §I(번호 안 줌 — 근거 §H 머리) · 결정 기록 색인 «근거 수준» 표 행마다 생성 조건 · ADR-003 · ADR-006 «다시 열리는 경우 ②» 줄 끝 · `embed_vs_bigram.cos` · `embed_model_sweep` 지역 `cos` → `memory._same_dim`(기록 경로 전부 1024 × 1024 · 전/후 출력 diff 0) · 새 시험 `test_cos_copies.py` 6 · `test_output_wording.py` 3(사본에 심은 위반 11/11 기대대로) · 시험 prototype 234 · experiments 576 · `run_all` 34건 · 실패 0 · 감사 지적 1 · SKIP 0 · 동결 해시 · 체크포인트 27 · 지문 불변 · 스냅샷 `.omc/plans/baseline/after-w11/` · 노트 `.omc/notepads/w12close/progress.md`.

## w14close — 실험 34(외부 인간 대화 · AI Hub 71630) 두 레인(w14a · w14b) 마감 (2026-09-12)

- 레인 노트를 옮기지 않고 재현기 둘을 다시 돌려 대조했다(w14a JSON 바이트 동일 · w14b JSON `cost` 네 칸만 다름 · 캐시 적중 100 % · 워크시트 라벨 0/40 확인 뒤 바이트 · 수정 시각 되돌림) · A0 칸을 등록 모듈만으로 세 번째로 계산해 두 레인과 정수로 같음을 확인 · 목록 해석을 정본으로 세 팔(A0 · N1 · D1)을 한 표에 · 등록 결함 넷(P2 닿을 수 없는 문턱 · `∪` 해석 · 모호 셋 · 분위 단위)과 동점 규칙이 판정을 만든 몫(낙관 순위 사후 표지)을 같은 칸에 → docs/11 «실험 결과 (34)»(번호 줌 — 근거 §0) · `실험 개수` 34 · `키 없이 도는 실험` 28(음성 대조 둘) · decisions-0911 §2 · §4 방아쇠 넷과 범위 줄 둘 끝 · 결정 기록 셋 «다시 열리는 경우» 끝 · 색인 «근거 수준» 표 네 행 · README · SUMMARY rev17 · experiments/README · NEXT §2″ · §5-18 · 새 시험 `experiments/test_aihub_cross.py` 5(심은 위반 10/10) · `test_aihub_dense.py` 단언 글자 나눔(«생성 호출 파일» 11 → 10 · 바닥 여유 제거) · 시험 prototype 234 · experiments 656 · `run_all` 34건 · 실패 0 · 감사 지적 1(알려진 1) · SKIP 0(최종 실행 — 첫 실행 뒤 시험 단언 글자를 고쳐 다시 돌림) · 누출 검사(이 라운드가 쓴 저장소 파일 전부) 0조각 · 동결 해시 · 체크포인트 27 · 지문 불변 · 스냅샷 `.omc/plans/baseline/after-exp34/` · 노트 `.omc/notepads/w14close/progress.md`.

## w23close — Fable 독립 검토 · 수리 중 닫힌 것 · 실험 34 후속 셋을 원장에 기록 (2026-09-12)

- 레인 노트를 옮기지 않고 다시 뽑았다 — 후속 재현기 셋(tie · m3 · dense)을 소켓 가드 아래서 다시 돌려 JSON 바이트 동일(비로컬 0 · m3만 로컬 엔진 접속 74 · 새 토큰 · 벡터 0) · 닫힌 수리 아홉의 시험 35건 초록 + 항목마다 새 변이 발화(9/9 · 변이 없는 사본 35 OK) · `repro.py` · `repro2.py`를 지금 트리 사본에서 다시 돌려 w21 출력과 같음 → docs/11 «실험 결과 (34)» 부록 I(번호 없음) · decisions-0911 θ ② · ⑤ · 엔진 ③ · ④ 줄 끝 · docs/17 §18(행 33 — ✅ 9 · 🔓 10 · ⏸ 12 · 🔄 2) · 닫힌 항목의 옛 문장 두 자리 + «삭제 전파 닫힘» 일곱 자리에 사실 한 구절 · NEXT §2‴(사용자 결정 Q1~Q7 → 묶음 A 나머지 · C · E). 코드 무변경 · `실험 개수` 그대로 · 노트 `.omc/notepads/w23ledger/` · 스냅샷 `.omc/plans/baseline/after-fable-0912/`.

## w25e — Fable 수리 계획 묶음 E 마감: 끝-끝 회귀 시험 · 원장 · 전량 회귀 (2026-09-12)

- 결정 Q1~Q7(전부 Fable 추천) 뒤 네 레인(w24a · w24c · w24d · w24b2)의 노트를 옮겨 적지 않고 다시 돌려 대조했다 — 🆕 `prototype/test_e2e_deletion_roundtrip.py`(계획 여덟 단계 + 6′ · 스위치 두 값 · 제품 함수만 · 심은 위반 다섯이 기대 단계에서 울고 모듈 하나만 착수판으로 되돌려도 다른 몫의 단계가 운다 · 계획 정정: 스위치 갈림은 4가 아니라 6′) · `repro.py` · `repro2.py` 기대 델타 전 행 일치 · G17′-a 집행 모집단 = ADR 문장(데모 규칙을 지운 사본에서 발화) · `fact` UNIQUE에 주어가 없는 결함 재현(🔓 · 안 고침) · tie · dense 재현 JSON 바이트 동일 → docs/17 §18-1 행 끝 28 · §18-5(끝-끝 표 · repro 표 · 🔓 여섯 · 계획 변경 · 세면 ✅ 15 · ⏸ 12 · 🔓 1 · 🔄 5) · 검§1-10 · ADR-016 U12 · U8 · G17′-a · ADR-004 멱등성 줄 · NEXT §0 · §2‴ · SUMMARY rev18 · README · decisions-0912 진행 기록. 제품 코드 무변경 · 시험 prototype 295(ollama 닫음 · skip 5) · experiments 759 · `run_all` 34건 · 실패 0 · 감사 지적 1 · SKIP 0 · 동결 해시 · 체크포인트 27 · 지문 불변 · `실험 개수` 그대로 · 노트 `.omc/notepads/w25e/` · 스냅샷 `.omc/plans/baseline/after-fable-fixes-0912/`.

## w33k — Fable 계획 2 묶음 K 마감: 끝-끝 9~11 · 원장 확정 · 전량 회귀 (2026-09-12)

- 결정 2(Q8~Q11 · 전부 Fable 추천) 뒤 레인 여섯(w30g · w31f · w31h · w31i · w31l · w32)의 보고를 옮겨 적지 않고 다시 돌려 대조했다 — w31f 변이 16판의 운 시험 집합이 w31f 출력과 같다(차이는 기대 교체 둘 · 새 끝-끝 메서드뿐) · w31h 변이 러너 정규화 출력 한 줄도 안 다름 · 🆕 끝-끝 `test_first_generation_refused_then_retried_abandoned_and_reset`(단계 9 · 9′ · 10a~10c · 11a · 11b · 11q · 스위치 두 값 · 제품 함수만 — 심은 위반 × 우는 단계 표 docs/17 §18-6-9) · 기대가 바뀐 기존 시험 둘 교체(수리 전 사본 옛 기대 초록 · 수리 뒤 새 기대 초록 · 옛 기대로 되돌린 변이 빨강 · 목적 단언 둘 더함) · `memory.py` 동점 주석 줄 수 중립 교체(주석만) · `regen_job.py` 문턱 주석 줄 끝(N6) · 원장(docs/17 §18-6 행 끝 + 18-6-7~11 · ADR-007 B-4 · ADR-015 결함 3행 · ADR-016 결정 ③ · U8 · U11 · U12 · 결정 문서 Q1 (a) · 대장 18 · 19 시점 표지).
- 🔴 계획 정정 셋: 경계 4 서빙의 «stale 기록 1»은 M=1에서 0(표지가 후보에도 안 오름) · «서빙 S01 블록»은 이 방에서 lifetime 블록으로만 · 한 subTest에 경계 셋을 넣으면 뒤 단계가 연쇄로 운다(경계마다 subTest로 가름). 곁 관측 둘(실패 기록 사유 = 예외 전체 · 표지 행이 경계 계수에 듦)은 재실행으로 확인 · 판단은 Fable 최종.
- prototype 323 OK(skipped 5 = 전부 «옵트인 아님 — 선언된 안 돌림» · ollama 닫음) · experiments 759 OK(skipped 3) · 동결 해시 · 체크포인트 27 · 지문 불변 · `실험 개수` 그대로 · 노트 `.omc/notepads/w33k/` · `run_all` · 스냅샷은 NEXT §0.
- 🔄 (w35 · 2026-09-12 · w34 Fable 최종 재검증 2 뒤 기록 — 문서 · 주석만) w34(`.omc/notepads/w34final2/final.md`)는 처분이 원장과 다른 행 0 · 새 변이 25종 전부 발화 · ✅ 15행 회귀 · 대장 표본 10행 날조 0 · PLAUSIBLE 🟢 둘 · 시험 구멍 하나(표지 파생 등록의 «지운 것 제외»를 끝-끝 9만 잡음 → 🔓). w35가 적은 자리: ADR-016 U6(표지 행이 경계를 맞게 셈 · 옛 전파 켬에서 `stale_expired` 첫 발화) · U8(K는 에피소드당 · 같은 세션 ① 재호출은 상한 밖) 행 끝 · `prototype/summarize.py` `hold_session` 주석 일곱 줄(AST · 코드 객체 동일) · docs/18 D-26 · D-28 · D-30 시점 표지 · docs/17 §18-6-12 · NEXT · SUMMARY · 결정 문서 한 줄. 동작 코드 0 · `run_all` 안 돌림(다음 실행에서 θ 격자 사본 sha의 `summarize.py` 줄만 움직인다) · 노트 `.omc/notepads/w35/`.
