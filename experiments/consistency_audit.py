# -*- coding: utf-8 -*-
"""
consistency_audit.py — 문서 45편에 흩어진 숫자가 서로 어긋나지 않는가. (API 불필요)

## 왜

실험을 20개 돌렸고 그때마다 문서를 고쳤다. 같은 숫자가 **여러 문서에 인용**된다.
[실험 13](../docs/11-experiment-results.md)의 회상률은 한 번 54%였다가 73%가 됐고,
[실험 12](../docs/11-experiment-results.md)의 θ는 0.15였다가 0.05가 됐다.

**한 곳을 고치고 다른 곳을 안 고치면 문서가 서로 다른 말을 한다.**
링크 검사는 하고 있지만 **숫자 검사는 안 하고 있었다.**

## 방법

핵심 수치를 **정규식으로 전수 수집**하고, 같은 주장에 대해 **서로 다른 값**이 있으면 보고한다.
자동으로 고치지는 않는다 — 맥락에 따라 옛 값을 일부러 남긴 곳이 있다(🔄 정정 표시).
**사람이 봐야 할 곳을 좁혀주는 것**이 목적이다.
"""
import re
import sys
from collections import defaultdict
from pathlib import Path

sys.stdout.reconfigure(encoding="utf-8")
W = 78

# (주장 이름, 정규식, 기대값 또는 None)
# 기대값을 적어두면 **그것과 다른 것만** 보고한다.
# 🔴 첫 판은 정규식이 헐거워 **무관한 숫자를 잔뜩 잡았다.**
#    "Oracle 회상률"이 "100%"를, "L3"가 04문서의 인용구를 잡는 식이었다.
#    -> 주장은 **문맥까지 포함한 정확한 문자열**로만 찾는다.
#       느슨하게 많이 잡느니 **좁게 확실한 것만** 본다.
CLAIMS = [
    ("제안안 응답 품질", r"19/26", None),
    ("Oracle 응답 품질", r"17/26", None),
    ("L3 활용 실패", r"L3 활용 실패[^\n]{0,14}?(\d{1,2})%|L3\((\d{2})%\)", "35"),
    ("콜드스타트 환각 상한", r"83%", None),
    ("θ 임계 (현행)", r"θ\s*=\s*0\.05", None),
    ("주입 공격 성공", r"0/45", None),
    ("stale 누수 수정", r"23(?:턴)?\s*(?:→|->)\s*0", None),
    # 🆕 실험 20 (ADR-014). 둘 다 **문맥 문자열을 붙여** 좁게 잡는다 —
    #    맨 `20/20`이나 맨 `4`는 다른 표의 숫자를 잔뜩 물어 온다.
    ("FSM 불법 주입 차단", r"20/20\s*차단", None),
    # 🔄 단계 0 (라운드 2) — **기본셀은 `G0 · θ=0.05`가 아니라 `G3 · θ=0.05`였다**(F21).
    # `memory.py:1007-1026`의 `Memory.gate`는 내용어 접점 게이트이고, 462개 발화에서
    # `gate_sweep.g_content`(G3)와 판정 불일치가 **0건**이다. 값 `4`는 옳았고
    # **그 값이 어느 구성의 것인지가 틀렸다** — 프로덕션 조합의 값은 `11`이다.
    # 실행 구성을 CLAIM 문자열 **안**에 박는다(G15). 그래야 다음에 셀이 바뀔 때
    # 문서와 감사기가 조용히 같이 틀리지 않고 **감사기가 먼저 운다.**
    ("정밀도 misinjection@q 기본셀(G3·θ=0.05)",
     r"misinjection@q[`\s]*기본셀\(G3·θ=0\.05\)\s*(\d+)", "11"),
    # 🆕 단계 0 (라운드 2 · F33) — 소크 저장량. `210B / 62KB월`이 두 문서에 남아
    # 있었는데 **CLAIM이 없어 감사기가 못 봤다.** 현재값은
    # `DB 204.0KB / 720턴 = 턴당 290B` · 월 300턴 = 85KB
    # (`baseline/after-step2/G2-soak.txt`).
    # ⚠️ 뒤의 `[,·]`가 규정의 일부다 — 없으면 `docs/adr/ADR-010:55`의
    # `턴당 200B × 월 10,000턴 = 2MB`를 함께 문다. 그것은 소크 실측이 아니라
    # **보존정책 문서의 설계 가정**이고, 낱말은 같지만 **다른 주장**이다.
    # 🔄 요약 계층 S1 (스키마 v5) — **290 → 302.** `digest_session` 테이블이
    #    생기면서 **행이 0인데도** DB가 8,192B(= SQLite 페이지 2개: 테이블 루트
    #    + PK 인덱스 루트) 커진다. 204.0KB → 212.0KB → 720턴으로 나눠 302B.
    #    🔴 **동작은 한 줄도 안 움직였다** — 블록·회상·게이트·컨텍스트 토큰 전부 동일.
    #    빈 테이블이 0바이트일 것이라는 가정이 틀렸고, 그것을 여기 적어 둔다.
    ("소크 저장량 턴당 바이트", r"턴당\s*\*?\*?(\d+)B\*?\*?\s*[,·]", "302", 2),  # 🔄 (n4) 최소 곳수 3 → 2 — 올리지 않는 SUMMARY.md 한 곳을 빼고 공개 파일만으로 센 값
    # 🆕 키 없이 도는 실험 수. README와 SUMMARY가 실제로 **어긋나 있었다**
    #    (README 13 vs SUMMARY 14 — 실험이 19→20으로 늘 때 한쪽만 고쳤다).
    #    "N개는 키 없이/키 불필요" 꼴은 두 문서에만 있으므로 좁다.
    # 🔄 라운드 2 단계 6b — **14 → 15.** 실험 21(검색 격자)이 늘었고 그것은
    #    API 키를 쓰지 않는다(로컬 ollama · `run_all.STEPS`의 `needs_extra=False`).
    #    ⚠️ 이 값과 `실험 개수`는 **함께** 움직인다 — 한쪽만 고치면 문서가
    #       "실험 21개 = 14 + 6"이라고 말하게 되고, **이 감사기는 그 덧셈을 못 본다.**
    # 🔄 후속 라운드 (레인 J) — **15 → 17.** 실험 22(유형 라벨 프로브)와
    #    실험 23(인용 감사)이 늘었고 **둘 다 API 키를 안 쓴다**: 22는 로컬
    #    ollama(bge-m3)라 `needs_extra=True`지만 그것은 *키*가 아니라 *의존성*이고,
    #    23은 완전 오프라인이다. 🟢 **이 CLAIM이 실제로 발화했다** — 실험 개수를
    #    21→23으로 올리고 이 값을 안 고친 중간 상태에서 감사가
    #    `기대 15 · 다른 값 발견 17`로 멈췄다. 그것이 이 CLAIM이 있는 이유다.
    # 🔄 레인 M·N 마감 — **18 → 19.** 실험 25(Q2-2 재설계)가 늘었고 그것도
    #    로컬 `qwen3:8b`만 쓴다(`needs_extra=True`는 *의존성*이지 *키*가 아니다).
    #    🟢 **이 CLAIM이 또 발화했다** — 실험 개수를 24→25로 올리고 이 값을 안
    #    고친 중간 상태에서 감사가 `기대 18 · 다른 값 발견 19`로 멈췄다.
    # 🔄 실험 26 라운드 — **19 → 20.** 채점기 검증(`scorer_eval.py`)이 늘었고 그것도
    #    로컬 ollama(`bge-m3`)만 쓴다. 캐시가 차 있으면 **호출 0회**로 돌지만 그 캐시는
    #    추적되지 않으므로 러너에서 `needs_extra=True`이고, 그것은 여전히 *의존성*이지
    #    *키*가 아니다. **옛 값 19는 `docs/11` 머리말의 🔄 정정 줄에 남아 있다** —
    #    여기서만 고치면 왜 움직였는지가 사라진다.
    # 🔄 실험 27 라운드 — **20 → 21.** 요약 채점(`summary_prototype.py`)이 늘었고
    #    그것도 로컬 ollama(`qwen3:8b`)만 쓴다 — lifetime 요약 **1건**을 만들고,
    #    `LIFETIME_S27.json`이 있으면 생성 0건으로 돈다. 러너에서 `needs_extra=True`
    #    인 것은 여전히 *의존성*이지 *키*가 아니다.
    #    **옛 값 20은 `docs/11` 머리말의 🔄 정정 줄에 남아 있다** — 여기서만 고치면
    #    왜 움직였는지가 사라진다.
    # 🔄 실험 28 라운드 — **21 → 22.** 요인 분해(`summary_ablation.py`)가 늘었고
    #    그것도 로컬 ollama(`qwen3:8b`)만 쓴다 — 요약 **3건**을 만들고,
    #    `ABLATION_S28.json`이 있으면 생성 0건으로 돈다. 러너에서 `needs_extra=True`
    #    인 것은 여전히 *의존성*이지 *키*가 아니다.
    #    **옛 값 21은 `docs/11` 머리말의 🔄 정정 줄에 남아 있다.**
    # 🔴 **네 번째 원소(최소 곳수)를 여기에 붙인다.** 이 CLAIM은 오늘 4곳에서
    #    맞는데, 그 곳수가 줄어도 값이 맞으면 감사는 초록이다 — 아홉 번째
    #    «발화할 수 없는 검사»가 정확히 그 형태였다(같은 파일의 `소크 저장량`
    #    주석). 문서 하나에서 정규식이 눈이 멀면 **이제 운다.**
    # 🔄 실험 29 라운드 — **22 → 23.** 게이트 포화(`gate_saturation.py`)가 늘었고
    #    그것은 **키도 의존성도 안 쓴다** — ollama를 한 번도 부르지 않는다
    #    (러너에서 `needs_extra=False`). 앞의 셋(26·27·28)은 *"의존성은 있고 키는
    #    없다"*라 이 CLAIM에 들어왔는데, **이번 것은 둘 다 없어서** 들어온다.
    #    **옛 값 22는 `docs/11` 머리말의 🔄 정정 줄에 남아 있다** — 여기서만
    #    고치면 왜 움직였는지가 사라진다.
    # 🔄 일반화 라운드 마감 — **23 → 25.** 둘이 한꺼번에 늘었다:
    #    `hardcoding_audit.py`(실험 30)와 `corpus2_probe.py`(실험 31). 🔴 **둘 다
    #    키도 의존성도 안 쓴다** — 30은 캐시된 생성물(`DRIFT_RESULTS`·
    #    `SUMMARY_LOCAL`·`LIFETIME_S27`)을 다시 채점할 뿐이고, 31은 코퍼스 생성도
    #    채점도 규칙 기반이라 LLM을 **한 번도** 부르지 않는다. 러너에서 둘 다
    #    `needs_extra=False`다 — 실험 29와 같은 부류이고, 이 CLAIM이 «의존성은
    #    있고 키는 없다»와 «둘 다 없다»를 구별하지 않는다는 것도 그대로다.
    #    **옛 값 23은 `docs/11` 머리말의 🔄 정정 줄에 남아 있다.**
    #    🟢 **이 CLAIM이 또 발화했다** — 문서 쪽을 25로 올리고 여기를 안 고친
    #    중간 상태에서 감사가 `기대 23 · 다른 값 발견 25`로 멈췄다. 🔄 실험 32 라운드 — **25 → 26** (줄을 안 늘리려 이 줄 끝에 붙인다 — 아래 `TIMELINE_PREFIX` 주석의 자기 인용이 밀리지 않게). 엔진 요인 분해(`engine_arms.py`)는 API 키를 안 쓰고 컨테이너 둘이 *의존성*이다(`needs_extra=True` · 없으면 77) — 26·27·28과 같은 부류. 🟢 **또 발화했다** — 문서를 26으로 올리고 여기를 안 고친 중간 상태에서 `기대 25 · 다른 값 발견 26`으로 멈췄다. 옛 값 25는 `docs/11` 머리말 🔄 정정 줄에 있다. 🔄 실험 33 라운드(w9close) — **26 → 27** (같은 줄 끝 규약). θ 격자(`theta_grid.py`)는 **키도 의존성도 안 쓴다**(LLM·임베딩·엔진 0회 · 소켓 연결 시도는 첫 회에 종료 1 · `needs_extra=False`) — 29·30·31과 같은 부류. 🟢 **또 발화했다** — 여기를 27로 올리고 문서를 안 고친 중간 상태에서 `기대 27 · 다른 값 발견 26`으로 멈췄다(`.omc/notepads/w9close/consist_neg1.txt`). 옛 값 26은 `docs/11` 머리말 🔄 정정 줄에 있다. 🔄 실험 34 라운드(w14close) — **27 → 28** (같은 줄 끝 규약). 외부 인간 대화(`aihub_multisession.py` · `aihub_dense.py` — 한 사전 등록을 두 레인이 잰다)는 API 키를 안 쓰고 외부 데이터 · OpenSearch 컨테이너 · 로컬 ollama가 *의존성*이다 — 26·27·28·32와 같은 부류. 🔴 `run_all`에 **없다**(원문이 약관상 저장소 밖 · 없으면 77 — 근거와 반대 논거는 `docs/11` «실험 결과 (34)» §0). 🟢 **또 발화했다** — 여기를 28로 올리고 문서를 안 고친 중간 상태에서 `기대 28 · 다른 값 발견 27`로, 문서 한 곳(`docs/11` 머리)만 올린 중간 상태에서 `실험 개수` 33 · 34가 갈려 멈췄다(`.omc/notepads/w14close/consist_neg1.txt` · `consist_neg2.txt`). 옛 값 27은 `docs/11` 머리말 🔄 정정 줄에 있다.
    ("키 없이 도는 실험", r"(\d+)개는 키 (?:없이|불필요)", "28", 3),  # 🔄 (n4) 최소 곳수 4 → 3 — 올리지 않는 SUMMARY.md 한 곳을 빼고 공개 파일만으로 센 값
    # 🆕 라운드 2 단계 6b — **검색 격자의 셀 수.** 실험 21이 여러 문서에 인용되기
    #    시작했고(ADR-015 · docs/11 · SUMMARY), 셀 수는 그중 **가장 자주 옮겨 적히는
    #    숫자**다. 문맥 낱말 `격자`를 붙여 좁게 잡는다 — 맨 `30`은 다른 표를 잔뜩 문다.
    #    🔴 **콜드 p95는 CLAIM으로 만들지 않는다.** 그 값은 n=18에서 표본의 최댓값이라
    #       실행마다 갈린다(PASS 10 / FAIL 8). **재현되지 않는 숫자를 감사가 지키면
    #       감사가 잡음을 동결한다** — G11이 금지하는 것이 정확히 그것이다.
    ("검색 격자 셀 수", r"격자\s*\*{0,2}(\d{2})\*{0,2}셀", "30"),
    # 🆕 요약 계층 S5 (ADR-016) — **서빙 정책 델타.** 이 라운드가 판정한 유일한
    #    값이고, 벌써 세 편에 옮겨 적혀 있다(ADR-016 · docs/11 부록 · docs/adr/README).
    #    `fsm_probe.py`가 실행마다 다시 뽑는 수이므로 G11 대상이다.
    # 🔴 **화살표가 규정의 일부다.** 맨 `제외 (\d+)회`로 잡으면 안 된다 —
    #    같은 문서들이 **새 기본값의 값(`제외 0회`)도** 적기 때문에 옛 동작의
    #    `10`과 새 동작의 `0`이 한 CLAIM에 섞여 감사가 정상인 것을 빨갛게 만든다.
    #    `주입 0회 → N회` 꼴은 **델타를 적는 자리에만** 나타난다.
    # ⚠️ 이 CLAIM은 `docs/adr/ADR-013:139`의 **앵커를 지키지 않는다** — 그 줄은
    #    델타가 아니라 옛 동작의 값 하나만 적는다. 그 자리는 `fsm_probe.py`가
    #    실행 중에 읽어서 지킨다(못 읽으면 종료 1). **두 방어가 다른 자리를 본다.**
    ("서빙 정책 델타 주입(옛→새)", r"주입\s*0회\s*→\s*\*{0,2}(\d+)\*{0,2}회", "10"),
    # 🆕 괴리 처분 (마지막 파도) — **엔진 라운드를 미룬 근거의 수.** docs/17 §9 ·
    #    ADR-015 «미해결» · docs/11 부록 · SUMMARY · experiments/README에 옮겨 적혔다.
    #    `/18`이 규정의 일부다 — 다시 여는 조건(«≥ 6»)은 분모가 없어서 안 문다.
    #    값은 `.omc/plans/baseline/after-gap-round/df_probe.py`가 다시 뽑는다(G11).
    #    ⚠️ 최소 곳수 7 — 한 문서에서 정규식이 눈이 멀면 운다(아홉 번째의 교훈).
    ("엔진 라운드 — IDF가 움직일 수 있는 문항", r"IDF가 (?:순위를 )?움직일 수 있는 문항[^\n/]{0,40}?\*{0,2}(\d+)/18", "3", 7),
    # 🆕 축 라운드 (마감 파도) — **θ 위치 판정의 수.** 격자 2단의 가중치 축에서 주입 집합이 바뀐
    #    문항(분모 36 = 게이트 통과 12 × 2단 3구성)이 docs/17 · docs/11 · ADR-015 · SUMMARY · README ·
    #    experiments/README에 옮겨 적혔다. `theta_position.py`가 실행마다 다시 뽑는다(`run_all`에 있다).
    #    `False … 0/36`은 정규식에 **박았다** — 값은 True 쪽만 잡히므로 False 쪽이 바뀐 곳은 안 잡히고,
    #    그러면 **최소 곳수 9**가 운다. 🔴 지연 교차점(≈ 3,000행)은 CLAIM으로 만들지 않는다 — 콜드 p95와 같은 이유다. 🔄 (w9close) 이 값은 **21행 코퍼스의 값**이다 — 규모 코퍼스에서는 스위치를 끈 채로도 가중치 축이 움직인다(실험 33 F). 옛 문장들에 줄 끝 🔄 정정을 붙였을 뿐 값 18 · 곳수 9는 그대로다. 🆕 (w9close) 바로 아래 줄 끝에 **실험 33의 머리 수** CLAIM을 붙였다 — 현행 조합에서 «켬 나음»인 열 수(분모 5 = 코퍼스 열)가 docs/11 · docs/17 · ADR-005 · SUMMARY · README · experiments/README에 옮겨 적혔고 `theta_grid.py`가 `run_all`에서 다시 뽑는다. `/5열`이 규정의 일부다(«켬 나쁨»이나 분모 없는 «0칸»은 안 문다) · 최소 곳수 9 · 음성 대조 `.omc/notepads/w9close/claim_neg.txt`. 🔴 줄을 늘리지 않으려 **같은 줄에** 붙였다 — 아래 `TIMELINE_PREFIX` 주석의 자기 인용이 밀리지 않게.
    ("θ 위치 — 가중치 축 True 바뀐 문항",
     r"가중치 축 False\s*\*{0,2}0/36\*{0,2}\s*·\s*True\s*\*{0,2}(\d+)/36", "18", 8), ("θ 격자 — 현행 조합 «켬 나음» 열", r"«켬 나음»[^\n/]{0,40}?\*{0,2}(\d+)/5열", "0", 10),  # 🔄 (w10record) 최소 곳수 9 → 10 — ADR-005 «미해결» 끝 사용자 결정 줄이 한 곳을 더했다(음성 대조 노트) · 🔄 (n4) θ 위치 최소 곳수 9 → 8 — 올리지 않는 SUMMARY.md 한 곳을 빼고 공개 파일만으로 센 값
]

# 세는 것 — 늘면 여러 곳을 같이 고쳐야 한다. **여기가 실제로 어긋난다.**
COUNTS = [
    # 🔴 라운드 2 마감 — `\*{0,2}`가 규정의 일부다. 굵게 표시(`실험 **19건**`)가
    #    정규식을 갈라 놓아 `REPORT.md:967-968`의 낡은 편수를 **아무도 못 봤다.**
    #    선행 라운드의 문서 동기화가 `결정 기록 13편`을 문자 그대로 찾았는데
    #    실제 문자열은 `결정 기록 **13편**`이라 **히트가 0이었다** — 이 저장소의
    #    여섯 번째 "발화할 수 없는 검사"다.
    ("실험 개수", r"실험\s*\*{0,2}(\d+)\*{0,2}개"),
    ("ADR 편수", r"(?:ADR|결정 기록)\s*\*{0,2}(\d+)\*{0,2}편"),
]


# 시간순 기록물은 **옛 값이 남아 있는 게 정상**이다.
#   PROGRESS.md   그때의 상태를 적은 로그다. "실험 10개"는 그 시점에 참이었다
#   docs/00·01    초기 계획서다. "ADR 6편 작성"은 계획이지 현황이 아니다
# 이걸 안 빼면 감사가 **정상인 것을 계속 빨갛게** 만들고, 그러면 아무도 안 본다.
#   REPORT.md    **이미 확정된** 옛 보고서다. 그때의 수치(설계 17편·결정 13편·
#                실험 19건)가 확정 시점에 참이었고, 지금 고치면 **확정한 것과
#                다른 문서**가 된다. 고칠 대상이 아니라 얼려 둘 대상이다.
TIMELINE = {"PROGRESS.md", "docs/00-research-briefing.md",
            "docs/01-problem-and-hypotheses.md", "REPORT.md"}

# 같은 논리를 **디렉터리 통째로** 적용해야 하는 곳.
#   .omc/plans/**   계획 문서다. "ADR 3편 추가"라고 쓰면 감사가 그걸 **ADR 편수
#                   주장**으로 읽고, "실험 20개 → 21개"라고 쓰면 실험 개수가
#                   어긋난 것으로 읽는다. 계획은 현황이 아니라 **의도**다.
# 정상인 것을 빨갛게 만들면 아무도 감사를 안 본다 — 원 주석의 논리를 그대로 적용한다.
# ⚠️ `:193`이 `m.as_posix().lstrip("./")`로 비교하므로 앞 점이 지워진다.
#    (🔄 이동 대장 — CLAIM이 늘 때마다 밀린다: `:150` → `:160`(실험 28)
#     → `:170`(실험 29) → `:180`(일반화 라운드) → `:186`(괴리 처분) → `:193`(축 라운드). **줄을 옮기는
#     편집은 자기 자신의 인용을 깨뜨린다** — 실험 23이 적은 그 관측이 또 났다. 🔴
#     **고치는 편집이 또 줄을 민다** — 앞 라운드는 되먹임을 두 번 돌아 잡았고, 그
#     뒤로는 **대체 블록의 줄 수를 원본과 같게(6줄) 맞춰** 한 번에 닫는다.)
#    `(".omc/",)`만 넣으면 `"omc/plans/…"`와 매칭되지 않는다 — **두 형태를 모두 받는다.**
TIMELINE_PREFIX = ("omc/", ".omc/")


def main():
    root = Path(".")
    mds = [m for m in sorted(root.rglob("*.md"))
           if (p := m.as_posix().lstrip("./")) not in TIMELINE
           and not p.startswith(TIMELINE_PREFIX)]
    print("=" * W)
    print("문서 정합성 감사 — 같은 주장에 다른 숫자가 있는가")
    print("=" * W)
    print(f"\n대상 {len(mds)}편. 링크는 따로 검사한다 — 여기서는 **숫자**만 본다.\n")

    issues = 0
    for claim in CLAIMS:
        # 🆕 요약 계층 마감 — **네 번째 원소는 «최소 곳수»다** (선택).
        #
        # 🔴 검증자가 이 라운드에서 **아홉 번째 «발화할 수 없는 검사»**를 찾았다:
        #    `SUMMARY.md`의 `턴당 **302B**(🔄 …)` 에서 괄호가 정규식이 요구하는
        #    구분자 `[,·]`를 밀어내 **커버리지가 3곳 → 2곳으로 조용히 줄었다.**
        #    값은 여전히 302라 감사는 `✅ (2곳, 일치)`를 찍었고, **곳수가 줄어든
        #    것을 아무도 못 봤다.** 「굵게 표시가 편수 정규식을 가른」 여섯 번째
        #    사고와 같은 계보다 — 정규식이 눈이 머는데 초록이 유지된다.
        #
        #    그래서 곳수를 **주장으로 만든다.** 곳수가 기대보다 적으면 값이 맞아도
        #    운다. 이것이 «값은 맞는데 그것이 가리키는 범위가 줄었다»를 잡는 유일한
        #    자리다. (기대를 안 적으면 오늘처럼 값만 본다 — 기존 CLAIM은 그대로.)
        name, pat, expect = claim[0], claim[1], claim[2]
        min_sites = claim[3] if len(claim) > 3 else None
        found = defaultdict(list)
        for md in mds:
            for m in re.finditer(pat, md.read_text(encoding="utf-8")):
                v = next((g for g in m.groups() if g), m.group(0))
                found[v].append(md.as_posix())
        if not found:
            # 🔴 라운드 2 단계 0 — 여기는 원래 `continue`였고 `issues`를 안 올렸다.
            #    그래서 **정규식이 깨져도 감사는 종료 0**이었다. 이 저장소가 이미
            #    두 번 겪은 실패 모드다 — 발화할 수 없는 검사(rev1의 G17,
            #    로케일 때문에 항상 통과하던 라벨 폭 검사).
            #    CLAIM이 0곳이면 주장이 사라졌거나 정규식이 문서와 어긋난 것이고,
            #    **둘 다 사람이 봐야 한다.**
            print(f"  🔴 {name:<28} 어디에도 없음 — 주장이 사라졌거나 정규식이 어긋났다")
            issues += 1
            continue
        vals = sorted(found, key=lambda v: -len(found[v]))
        if expect and any(v != expect for v in vals if v.isdigit()):
            odd = {v: f for v, f in found.items() if v != expect}
            print(f"  🔴 {name:<28} 기대 {expect} · 다른 값 발견")
            for v, files in odd.items():
                shown = ", ".join(sorted(set(files))[:3])
                print(f"       {v:<8} {shown}")
            issues += 1
        else:
            n = sum(len(f) for f in found.values())
            if min_sites is not None and n < min_sites:
                print(f"  🔴 {name:<28} {vals[0]:<8} ({n}곳 — 기대 {min_sites}곳 이상)")
                print(f"       값은 맞는데 **커버리지가 줄었다** — 정규식이 문서 한 곳에서")
                print(f"       눈이 멀었거나 그 주장이 문서에서 사라졌다. 둘 다 사람이 본다.")
                issues += 1
            else:
                tail = f", 일치" if min_sites is None else f" ≥ {min_sites}, 일치"
                print(f"  ✅ {name:<28} {vals[0]:<8} ({n}곳{tail})")

    print("\n" + "-" * W)
    print("세는 값 — 늘어나면 여러 곳을 같이 고쳐야 한다")
    print("-" * W)
    for name, pat in COUNTS:
        found = defaultdict(set)
        for md in mds:
            for m in re.finditer(pat, md.read_text(encoding="utf-8")):
                v = next((g for g in m.groups() if g), None)
                if v:
                    found[v].add(md.as_posix())
        if len(found) > 1:
            print(f"  🔴 {name}: 서로 다른 값이 있다")
            for v in sorted(found, key=int, reverse=True):
                print(f"       {v:<4} {', '.join(sorted(found[v])[:3])}")
            issues += 1
        elif found:
            v = list(found)[0]
            print(f"  ✅ {name}: {v} (일치)")

    print("\n" + "-" * W)
    if issues:
        print(f"🔴 {issues}건을 사람이 확인해야 한다.")
        print("   ⚠️ 전부 오류는 아니다 — 🔄 정정 블록에서 **옛 값을 일부러 남긴** 곳이 있다.")
        print("      이 도구는 **볼 곳을 좁혀줄 뿐** 판단하지 않는다.")
    else:
        print("✅ 검사한 범위에서 숫자 불일치 없음.")
    print("-" * W)
    print("\n⚠️ 이 감사가 못 잡는 것")
    print("  · 정규식에 없는 주장. **목록을 손으로 유지해야 한다**")
    print("  · 숫자가 아닌 주장의 모순 (예: '검색이 기여한다' vs '기여 0')")
    print("  · 맥락이 달라 같은 숫자가 다른 뜻인 경우")
    print("=" * W)
    return 1 if issues else 0


if __name__ == "__main__":
    sys.exit(main())
