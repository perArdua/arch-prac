# -*- coding: utf-8 -*-
"""
probe_set.py — **유형 라벨이 붙은 프로브 집합 (n = 24)**. 데이터 전용 모듈.

## 왜 이 파일이 있나

`docs/adr/ADR-015-retrieval-modes.md` 핵심 3의 **하이브리드 재진입 조건 1**:

    bge-m3로 채점되는 유형 라벨 프로브 집합이 n ≥ 20으로 존재한다.

오늘 저장소에 있는 두 집합은 **어느 것도 세 요건을 동시에 만족하지 않는다.**

    retrieval_sim.PROBES   (retrieval_sim.py:104)  n=10 · 유형 라벨 **없음** · BM25 채점
    hybrid_sim.PARAPHRASE  (hybrid_sim.py:48)      n= 5 · 라벨 2종     · **e5-small** 채점

ADR-015가 그것을 *"존재하지 않는 계측기를 조건으로 걸었다"*고 적었다. 이 파일이
그 계측기다. **채점은 `probe_types.py`가 하고 여기에는 데이터만 있다** — 프로브
집합을 하니스에서 떼어 놓는 이유는 아래 ⚠️ 자기저작 편향 선언의 마지막 문단이다.

## 이 파일이 있는 자리 — `eval/`이 아닌 이유

`eval/fact-ledger.yaml` · `eval/questions.yaml` · `eval/corpus/corpus.jsonl`은
**불변이다(A8)**. 그리고 ADR-015 핵심 3은 이 작업에 대해 두 번 이렇게 적었다:

    프로브는 `eval/` 밖 실험 자산이므로 만드는 것은 A8과 무관하다.

그래서 `eval/` 아래에는 **파일을 하나도 만들지 않는다.** 이 파일은 `eval/`을
**읽기 전용으로만** 연다(`corpus_ids` · `ledger_ids`). 새 파일을 `eval/`에 넣는
것은 A8의 문자를 어기지 않지만, A8이 지키는 것은 *"평가 데이터를 손대서 지표를
움직이지 않는다"*는 습관이고 — **프로브는 질의(質疑)지 사실(事實)이 아니므로
평가 데이터가 아니다.** 사실을 새로 만들면 그것은 A8 위반이다. 여기서 만드는 것은
`eval/`에 **이미 있는** 근거를 가리키는 질문뿐이고, 그래서 아래 `validate()`는
**모든 gold id가 이미 존재하는지를 강제한다.**

## 🔴 자기저작 편향 선언 — 요구사항이지 예의가 아니다

**이 프로브 집합은 자기저작(self-authored)이다.** 이것을 쓴 사람은
`prototype/memory.py`의 검색 경로(`bigrams` · `coverage` · `tokens_fixed` ·
`jaccard` · θ 컷 · 가중합)와 ADR-015의 결과 표를 **먼저 읽었다.** 그러므로:

1. **의역형이 얼마나 어려운지를 저자가 고를 수 있었다.** 색인을 본 사람이 쓴
   의역형 프로브는 우연히 **너무 쉽거나**(어휘가 살짝 겹치게) **불가능하거나**
   (어느 모드도 못 잡게) 될 수 있고, 그 방향은 저자가 원하는 답 쪽으로 기운다.
2. **유형 경계 자체가 저자 한 명의 판단이다.** 특히 **의역형/추론형 경계가 이
   집합에서 가장 약한 부분**이다(예: X001 "봄나들이" → "야외로 놀러") — 다른
   사람이 라벨을 다시 붙이면 몇 개는 옮겨 갈 것이다.
3. **E005·E006(두 번째/세 번째 면접)을 gold에서 뺐다.** 두 텍스트가 거의 같아
   **어떤 질의도 둘을 가를 수 없다** — 그것은 프로브의 성질이 아니라 코퍼스의
   성질이다. 뺀 것이 옳다고 보지만, **가장 어려운 두 칸을 저자가 뺐다**는 사실은
   그대로 남는다. `F002_INV`도 뺐다(무효화 레코드라 "근거"의 뜻이 다르다).
4. **저자가 어느 문항이 어느 모드에 유리한지 알고 썼다.** `lexical`이 글자
   겹침이라는 것을 알면 의역형을 *글자가 안 겹치게* 쓰는 것은 정의상 순환이다 —
   그것이 의역형의 정의이기도 하지만, **정의와 튜닝을 구별할 외부 기준이 없다.**

**무엇이 이것을 고치나 (이 라운드에서는 하지 않았다):**

- **코퍼스만 보고 쓴 프로브.** `prototype/memory.py`를 읽지 않은 사람이
  `eval/corpus/corpus.jsonl`만 보고 질의를 쓰고, 유형 라벨도 그 사람이 붙인다.
- **실제 사용자 질문에서 뽑은 프로브.** 이 저장소에는 그런 로그가 없다.
- **라벨의 2인 일치도.** 두 사람이 독립으로 라벨을 붙여 κ를 재면 위 2번이 숫자가
  된다. 지금은 숫자가 없다.

⚠️ **이 선언은 `.omc/plans/verifier-r2b-final.md`가 기록한 홀드아웃 부재 선언과
같은 지위다** — 그 선언은 ADR-015 본문에 살아 있고, 이 선언도 **조용히 빠지면 안
된다.** 그래서 `probe_types.py`는 실행할 때마다 이 선언을 **출력에 찍고**,
`test_probe_types.py`가 그것이 찍히는지 시험한다.

⚠️ **그리고 이 파일이 하니스와 분리된 이유가 여기 있다.** 위의 고침 중 첫째
(*"코퍼스만 보고 쓴 프로브"*)를 하려면 **이 파일만 갈아 끼우면** 된다 —
`probe_types.py`는 프로브를 하드코딩하지 않고 이 모듈에서 읽는다.

## 유형 분류 (taxonomy) — 세 유형과 그 근거

무엇을 가르려고 만든 축인가: **어휘 검색과 임베딩 검색을 가르는 축**이다.
그래서 축은 *"질의가 근거의 낱말을 얼마나 쓰는가"*여야 하고, 다음 셋이 된다.

| 유형 | 정의 | 어휘 검색에 대한 기대 |
|---|---|---|
| **직접형** | 질문이 근거의 낱말을 **그대로** 쓴다 | 잡아야 한다. 못 잡으면 어휘 검색이 자기 잘하는 것도 못 하는 것이다 |
| **의역형** | 뜻은 같고 **핵심 내용어가 겹치지 않는다** | 원리상 못 잡는다. 이 축이 존재하는 이유 |
| **추론형** | 낱말도 안 겹치고, 뜻도 그대로가 아니라 **한 단계 건너야** 한다 | 못 잡는다. 임베딩도 못 잡을 수 있다 — 그것이 이 유형이 따로 있는 이유 |

**추론형을 왜 넣었나.** 직접형/의역형만 있으면 *"임베딩이 의역형을 잡는다"*가
나왔을 때 그것이 **의미 유사도**인지 **분포적 근접성**인지 갈리지 않는다.
추론형은 임베딩의 상한을 재는 자리다 — 두 문장이 뜻이 통한다고 벡터가 가깝지는
않다. **임베딩이 의역형은 잡고 추론형은 못 잡는 것이 이 유형이 예상하는 결과이고,
그 예상이 빗나가는 것도 결과다.**

**8 / 8 / 8인 이유.** 유형별 부호 검정이 원리상 p ≤ 0.05에 닿으려면 유형당
n ≥ 6이어야 한다(양측 최소 p = 2/2^n — n=5면 **0.0625로 절대 못 닿고**, n=6이면
0.031). 8은 그 바닥 위의 첫 여유 눈금이고, 동시에 세 유형 × 8 = 24 ≥ 20이다.
**균형을 맞춘 이유는 읽을 수 있게 하기 위해서다** — 18직접형/2의역형짜리 집합은
자기가 존재하는 이유인 질문에 답할 수 없다.

**gold id 24개가 전부 서로 다르다.** 한 근거에 여러 질의를 붙이면 그 근거의
검색 난이도가 표본에 여러 번 들어가 n이 부풀려진다.

## 모집단

후보 집합은 **심긴 문서 27개**(`corpus.jsonl`의 `planted_id`가 있는 행)다 —
`embed_vs_bigram.py:103`과 **같은 모집단**이라 ADR-015의 4행 비교표와 나란히
읽을 수 있다. 🔴 **`rel_dist.py`의 378쌍(채점 18문항 × 색인 21행)은 다른
모집단이고 여기 값을 그리로 옮겨 적지 않는다**(P1 · G12). 프로브 표는
*"의역형을 잡는가"*를 묻고, 격자는 *"오늘의 코퍼스에서 근거를 꺼내는가"*를 묻는다.
"""
import json
import os
from collections import Counter, namedtuple

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
CORPUS = os.path.join(ROOT, "eval", "corpus", "corpus.jsonl")
LEDGER = os.path.join(ROOT, "eval", "fact-ledger.yaml")

# ── 유형 (taxonomy) ─────────────────────────────────────────────────────
# 🔴 **닫힌 집합이다.** 여기 없는 라벨은 `validate()`가 거부한다. 라벨을 자유
#    문자열로 두면 오타가 조용히 **새 유형**이 되고, 유형별 집계의 분모가 갈린다.
DIRECT = "직접형"
PARA = "의역형"
INFER = "추론형"
TYPES = (DIRECT, PARA, INFER)

# 사전 등록된 유형별 개수. 실제와 어긋나면 `validate()`가 거부한다 —
# 배분은 실행 전에 정한 것이고, 나중에 조용히 기울어지면 안 된다.
PLANNED = {DIRECT: 8, PARA: 8, INFER: 8}

Probe = namedtuple("Probe", "q gold type")

# ── 프로브 24개 ─────────────────────────────────────────────────────────
#
# 🔴 **순위를 보기 전에 고정했다.** 실행 후 질의를 고쳐 쓰지 않았다
#    (`.omc/notepads/laneI/progress.md`의 사전 등록 1번).
#
# 각 줄의 주석은 gold 근거의 원문이다 — 라벨이 맞는지 사람이 눈으로 대조할 수
# 있어야 한다. 그리고 **눈 대조만으로는 부족해서** `probe_types.py`가 유형별
# *정답쌍 어휘 rel*을 찍는다: 직접형이 높고 의역·추론형이 0 근처가 아니면
# **라벨이 틀린 것이다.** 라벨은 주장이고, 하니스가 그 주장을 검사한다.

TYPED_PROBES = [
    # ── 직접형 8 — 질문이 근거의 낱말을 그대로 쓴다 ──────────────────────
    Probe("고양이 이름이 나비였지", "F001", DIRECT),
    # F001 지우의 고양이 이름은 나비
    Probe("카페인에 민감해서 커피를 못 마신다고 했지", "F003", DIRECT),
    # F003 지우는 카페인에 민감해서 커피를 못 마심
    Probe("서준이 대학에서 2년 선배라고 했나", "F005", DIRECT),
    # F005 서준은 대학에서 지우보다 2년 선배
    Probe("나비가 아파서 응급실에 간 거", "E007", DIRECT),
    # E007 나비가 갑자기 아파서 응급실에 감
    Probe("이직 문제로 크게 다툰 거", "E008", DIRECT),
    # E008 이직 문제로 크게 다툼 — 서준이 "너 그러다 몸 상한다"고 했다가 감정 상함
    Probe("그 카페에 같이 가기로 약속했잖아", "D001", DIRECT),
    # D001 다음에 그 카페 같이 가기로 약속
    Probe("민지네 고양이 이름이 코코였나", "X003", DIRECT),
    # X003 친구 민지네 고양이 이름은 코코
    Probe("점심에 크로와상을 먹었다고 한 거", "F007_TRAP", DIRECT),
    # F007_TRAP 지우가 점심에 크로와상을 먹었고 "더 먹고 싶었는데 아쉽다"고 말함

    # ── 의역형 8 — 뜻은 같고 핵심 내용어가 겹치지 않는다 ─────────────────
    Probe("지우가 새 직장으로 옮긴 거 말이야", "F021", PARA),
    # F021 지우는 스타트업으로 이직함        (스타트업→새 직장 · 이직→옮김)
    Probe("지우가 하는 일이 광고 쪽이라고 했던가", "F002", PARA),
    # F002 지우는 마케팅 회사 대리            (마케팅→광고 쪽)
    Probe("지우 밑으로 형제가 하나 있다고 했지", "F004", PARA),
    # F004 지우에게 여동생이 한 명 있음        (여동생→밑으로 형제)
    Probe("지우가 직장에서 혼나고 밤늦게 전화했던 날", "E001", PARA),
    # E001 지우가 회사에서 크게 깨지고 새벽에 연락함
    Probe("서준이 지우 귀갓길에 같이 걸어준 날", "E002", PARA),
    # E002 서준이 지우 집 앞까지 데려다줌 (처음으로)
    Probe("결국 붙었다고 한 거", "E009", PARA),
    # E009 최종 합격                          (합격→붙다)
    Probe("당락이 정해지면 알려준다고 했잖아", "D002", PARA),
    # D002 면접 결과 나오면 바로 말해주기로 함
    Probe("너도 네 사정 언젠가 털어놓겠다고 했잖아", "D004_UNPAID", PARA),
    # D004_UNPAID 서준이 자기 얘기도 나중에 해주겠다고 함

    # ── 추론형 8 — 근거에서 한 단계 건너야 답이 나온다 ───────────────────
    Probe("지우가 지금 회사를 계속 다닐 생각이 없다고 처음 내비친 게 언제였지",
          "E003", INFER),
    # E003 지우가 이직 준비를 시작한다고 말함   (이직 준비 ⇒ 지금 회사를 떠날 생각)
    Probe("지우가 이직하면서 처음 좌절했던 순간", "E004", INFER),
    # E004 첫 번째 면접 — 탈락                  (탈락 ⇒ 좌절)
    Probe("우리 사이가 다시 괜찮아진 계기가 뭐였지", "E010", INFER),
    # E010 화해 — 서준이 먼저 연락              (화해 ⇒ 사이 회복 · 먼저 연락 ⇒ 계기)
    Probe("지우가 야외로 놀러 갔다 온 얘기 한 적 있나", "X001", INFER),
    # X001 지우가 봄나들이에서 나비를 봤다고 함  (봄나들이 ⇒ 야외 나들이)
    Probe("지우가 남 줄 물건을 산 적 있었지", "X002", INFER),
    # X002 지우가 나비넥타이를 선물로 샀다       (선물 ⇒ 남 줄 물건)
    Probe("지우가 우유 들어간 걸 마셨다는 게 언제였지", "X004", INFER),
    # X004 지우가 회사 근처 카페에서 커피 대신 라떼를 마셨다  (라떼 ⇒ 우유 든 음료)
    Probe("내 주변에 작은 회사 다니는 사람 얘기 한 적 있지", "X005", INFER),
    # X005 서준이 스타트업 다니는 선배 얘기를 함 (스타트업 ⇒ 작은 회사)
    Probe("지우가 태어난 날 뭐 할지 아직 안 알려줬잖아", "D003", INFER),
    # D003 생일에 뭐 하고 싶은지 나중에 말해주기로 함  (생일 ⇒ 태어난 날)
]

# 재진입 조건 1이 읽는 숫자. 리터럴로 박지 않는다 — 집합이 줄면 여기도 줄어야 한다.
N_MIN = 20

# 🔴 **출력에 찍히는 자기저작 선언.** 위 파일 머리말의 요약이고 **정본은 머리말**이다.
#    여기 상수로 둔 이유 하나: 이 문자열이 `probe_types.py`의 출력에 들어가고
#    `test_probe_types.py`가 그것을 시험한다. 선언이 파일 주석에만 있으면
#    **출력만 읽는 사람에게는 존재하지 않는다** — 홀드아웃 부재 선언이 ADR 본문에
#    남아야 했던 것과 같은 이유다(`.omc/plans/verifier-r2b-final.md`).
SELF_AUTHORSHIP = """🔴 자기저작(self-authored) 선언 — 이 집합의 한계는 제거되지 않았다

  이 프로브 24개는 `prototype/memory.py`의 검색 경로와 ADR-015의 결과 표를 **먼저
  읽은 사람이 썼다.** 그래서 (1) 의역형의 난이도를 저자가 고를 수 있었고 — 색인을
  본 사람이 쓴 의역형은 우연히 너무 쉽거나 불가능한 쪽으로 기운다 — (2) 유형 경계도
  저자 한 명의 판단이며 **의역형/추론형 경계가 가장 약하다**, (3) 서로 거의 같은
  텍스트인 E005·E006을 저자가 gold에서 뺐다(어떤 질의도 둘을 못 가른다는 이유이고
  그 이유는 옳지만, **가장 어려운 두 칸을 저자가 뺐다**는 사실은 남는다).

  무엇이 고치나: **코퍼스만 보고 `memory.py`를 읽지 않은 사람이 쓴 프로브**, 또는
  **실제 사용자 질문에서 뽑은 프로브**, 그리고 **두 사람이 독립으로 붙인 라벨의
  일치도(κ)**. 셋 다 이 라운드에서 하지 않았다.

  ⚠️ 이 선언은 ADR-015의 홀드아웃 부재 선언과 같은 지위다 — **조용히 빠지면 안 된다.**"""


# ── 코퍼스·원장 읽기 (읽기 전용 — A8) ──────────────────────────────────

def corpus_docs():
    """`corpus.jsonl`의 **심긴 문서**(planted_id가 있는 행)만. 후보 집합이다."""
    with open(CORPUS, encoding="utf-8") as f:
        docs = [json.loads(line) for line in f]
    return [d for d in docs if d["planted_id"]]


def corpus_ids():
    return {d["planted_id"] for d in corpus_docs()}


def ledger_ids():
    """
    `fact-ledger.yaml`의 id 전부 (facts · events · debts).

    코퍼스만 보지 않는 이유: gold는 *"근거"*이고 근거의 정본은 원장이다.
    `F002_INV`처럼 **코퍼스에만 있고 원장에 없는** 행이 실제로 있다 — 그것을
    gold로 쓰면 "이미 존재하는 사실을 가리킨다"는 이 파일의 전제가 깨진다.
    """
    import yaml
    with open(LEDGER, encoding="utf-8") as f:
        led = yaml.safe_load(f)
    out = set()
    for key in ("facts", "events", "debts"):
        for item in led.get(key) or []:
            if isinstance(item, dict) and "id" in item:
                out.add(item["id"])
    return out


# ── 검증 — 🔴 조용히 건너뛰지 않는다 ───────────────────────────────────

class ProbeSetError(ValueError):
    """프로브 집합이 스스로의 규약을 어겼다. **경고가 아니라 중단이다.**"""


def validate(probes=None, valid_ids=None, planned=None, n_min=None):
    """
    프로브 집합을 검사한다. 어긋나면 `ProbeSetError`를 **던진다.**

    🔴 **왜 예외인가 — 조용한 건너뜀이 분모를 깎기 때문이다.** gold id가 없는
       프로브를 조용히 빼면 n이 24에서 23이 되고, *"24개 중 몇 개"*라고 적힌
       모든 비율이 틀린 분모 위에 선다. 그것이 `hard_misinjection`이 죽은 방식과
       같은 부류의 실패다(도달 불가능한 항목이 분모에 남아 상수 0을 만들었다).
       **여기서는 반대 방향으로 같은 병이 생긴다** — 분모가 소리 없이 줄어든다.

    🔴 **모든 위반을 한 번에 모아서 던진다.** 첫 번째에서 멈추면 고치고 다시 돌리는
       일이 위반 수만큼 반복되고, 그 사이에 사람이 *"이제 됐겠지"* 하고 넘어간다.
    """
    probes = TYPED_PROBES if probes is None else probes
    planned = PLANNED if planned is None else planned
    n_min = N_MIN if n_min is None else n_min

    bad = []

    # 1. 구조 — 이 셋은 파일만 보고 알 수 있다 (eval/을 안 연다).
    if len(probes) < n_min:
        bad.append(f"n = {len(probes)} < {n_min} — 재진입 조건 1의 하한 미달")

    counts = Counter(p.type for p in probes)
    for t in counts:
        if t not in TYPES:
            offenders = [p.q for p in probes if p.type == t][:3]
            bad.append(f"분류 밖 유형 라벨 {t!r} — 허용: {list(TYPES)} "
                       f"(예: {offenders})")
    for t, want in planned.items():
        if counts.get(t, 0) != want:
            bad.append(f"유형 {t} 개수 {counts.get(t, 0)} ≠ 사전 등록 {want}")

    seen = Counter(p.q for p in probes)
    for q, c in seen.items():
        if c > 1:
            bad.append(f"질의 중복 {c}회: {q!r}")
    seen_gold = Counter(p.gold for p in probes)
    for g, c in seen_gold.items():
        if c > 1:
            bad.append(f"gold id 중복 {c}회: {g} — 한 근거가 표본에 여러 번 든다")

    # 2. gold 해석 — `valid_ids`를 안 주면 코퍼스 ∩ 원장을 쓴다.
    if valid_ids is None:
        valid_ids = corpus_ids() & ledger_ids()
    for p in probes:
        if p.gold not in valid_ids:
            bad.append(f"정답 근거 id {p.gold!r}가 존재하지 않는다 "
                       f"(질의 {p.q!r}) — 프로브는 질의를 쓰는 일이지 "
                       f"근거를 만드는 일이 아니다 (A8)")

    if bad:
        raise ProbeSetError(
            f"프로브 집합이 {len(bad)}건의 규약을 어겼다 — **조용히 건너뛰지 "
            f"않는다.**\n  - " + "\n  - ".join(bad))
    return True


def _selfcheck():
    """
    import 시점의 **구조** 검사. `eval/`을 열지 않는다 (import에 파일 I/O를 붙이면
    시험이 이 모듈을 못 불러온다). gold 해석은 `validate()`가 한다.
    """
    validate(valid_ids={p.gold for p in TYPED_PROBES})


_selfcheck()
